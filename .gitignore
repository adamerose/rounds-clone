# Raw research media stays out of git history permanently.
# Screenshots and video frames of the original game are reference input, not project assets.
# Anything committed here can never be removed without rewriting history, which would
# foreclose ever making this repo public. Only derived measurements belong in spec/.
research/raw/

# Rendered output
out/
reels/
.tools/

# Godot
.godot/
.import/
export_presets.cfg

# .NET
bin/
obj/
*.user
