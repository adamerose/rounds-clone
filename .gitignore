# User-supplied fidelity media stays local; only reference/manifest.json is tracked.
reference/*.mp4
reference/screenshots/*.png

# Rust output and generated evidence.
target/
out/

# Raw research inputs and local tools remain outside Git.
research/raw/
.tools/
