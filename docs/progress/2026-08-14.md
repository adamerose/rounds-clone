# 2026-08-14

The design-only seed is now an initialized Git repository with an isolated bootstrap worktree, hash-verified local .NET and Godot tooling, a pure deterministic simulation boundary, PCG state, stable state hashing, nine automated tests including realistic rule-bypass fixtures, repeatable headless smoke output, a loadable Godot C# shell, pinned CI references, and three accepted visual concept screens for the original `RICOCHET` presentation.
There is no replay reel yet because the bootstrap world intentionally has no researched movement or combat; replay capture and rendering remain the first visibility requirement after the simulation mechanics are specified.

The core research now targets public Windows build `21020021`, identified in-game as `v1.1.2.a75ee335a`, and records the complete five-point 1v1 match sequence plus base movement, combat, blocking, camera, and out-of-bounds targets.
Seven required JSON documents carry per-fact provenance, confidence, derivation methods, and tolerances, while 19 frame measurements preserve raw observations, visible loadouts, source timestamps, and recomputable arithmetic.
A dependency-free schema subset validator and eleven specification tests make missing provenance, dangling sources, required metric omissions, insufficient independent evidence, missing raw operands, inconsistent endpoint spans, false normalized results, and unsupported schema vocabulary fail the repository gate.
Complete vanilla card and map catalogs are bounded as tickets 003 and 004 before their corresponding implementation work.

The vanilla card catalog now binds 67 stable project IDs across 17 common, 33 uncommon, and 17 rare upgrades for build `21020021`.
Its 199 effect records separate visible modifiers from deterministic hooks and unresolved stacking formulas, preserve three official patch milestones, retain source conflicts, and order later implementation from stat-only folds through projectile, conditional, block-graph, and lifecycle behavior.
The repository gate now validates card schema shape, known stats and hooks, source completeness, exact count, unique IDs, current build identity, five representative stacking cases, patch provenance, source exclusions, operation units, and two-source numeric coverage through 22 checker tests.
Fresh reviewer session `codex:019fff67-9eba-7042-8e81-fc16c3885b45` approved exact candidate `02c339a89dfdbb3ecf276f886d82f064e5a4eda5`, which was fast-forwarded to `main` unchanged.

The vanilla arena catalog now binds 70 stable row-anchored project IDs to 1,792 clean-room oriented boxes, two source-supported spawn regions per map, bounded camera and kill regions, visible hazards, and explicit behavior ownership.
Its structural oracle preserves every connected source component, caps maps at 96 boxes, and scores an 80-by-45 occupancy grid without tracing source art, while a positioned-render digest catches geometry drift.
Current-build footage binds `arena-026` to two mirrored moving platforms through ten paired samples and a partial 840-tick endpoint-to-reversal interval without inventing the unobserved full period.
Fresh reviewer session `codex:019fffeb-75c7-7c30-82d5-ac46c0ec51a3` approved exact candidate `67369534652c9aac6e2fb278e6afdc09eab213a9` after independent regeneration, row matching, frame extraction, the zero-warning 46-test gate, deterministic smoke, and Godot editor/runtime smoke.
