---
format: 3
status: idea
created: 2026-09-07T17:39:20Z
origin: system-detected
tags: ["product-fidelity", "match-flow", "cards", "presentation", "bevy"]
value: 8
risk: 4
sessions:
  - claude:96849848-e6ec-488e-b4ac-b113acc49f8a
execution: unattended
depends-on: [46]
supersedes: []
split-from: [48]
---

# Run the first loser draft through flow and presentation

Ticket 046 leaves the connected match parked on `ROUND BLUE`. A public inspect trace proves how parked: run the `rematch-draft-replay` profile at seed 41 for 5,466, 5,500, 5,600, 5,700, 5,800, 5,893 and 5,894 ticks and every one of them reports phase `roundBlue` at phase revision 22 with the identical combat digest `7b86de3c86052476…` and byte-identical fighter positions, velocities, health and alive flags. `FlowAuthority::advance` has no match arm for `RoundBlue` or `RoundOrange`, and `apply` answers every `FlowCommand` in those phases with `WrongPhase`, so the round result is a terminal state the authority cannot leave. The source does leave it: 13 ticks later a card silhouette rises behind the overlay, 15 ticks after that the overlay and orange's losing half both vanish, and orange drafts `QUICK SHOT` from a five-card fan before the next arena arrives.

This ticket takes the physics-free prefix of blocked ticket 048 — the draft itself, and nothing after it. It ends at the last frame before the incoming hanging arena's first pixel, so no suspension body, ammunition mark, reload arc, stored jump, wall launch, momentum absorption or projectile recalibration is in scope. Those stay with 048 in its own worktree, where they are blocked on a hanging-arena suspension fit this ticket does not touch.

Why it matters: the coverage ledger currently calls the 04:10–04:20 draft unresolved and `S1-flow-draft`/`S5-card-combat` are the two slices with the least footage behind them. This interval is the whole of a card draft — offers, reveal cadence, two hovers, one confirmation, a loadout append and a half reset — and it needs no new physics, arena, transport or renderer path to deliver.

## Outcome

- `FlowAuthority` leaves `RoundBlue` and `RoundOrange` into a post-round draft in which exactly one player — the fighter who lost the completed round — is the active drafter, and the winner never drafts. The route reaches the draft from the existing 046 route through ordinary authority advance; the eleven bound source frames map to clone ticks 5466, 5478/5479, 5493/5494, 5588, 5710, 5801/5802, 5818 and 5893, and the authoritative cadence is: draft phase entered at 5494, `OVERPOWER` hover accepted at 5588, `QUICK SHOT` hover accepted at 5710, confirmation accepted at 5802, bridge phase entered when the selected face has left the fan, route ends at 5893. `record_elimination` still returns false in every one of these phases, and a `FlowCommand` from the winner, at a stale `phase_revision`, or naming an item outside the five offers is rejected with the existing `WrongPlayer`/`Stale`/`NotOffered` results without changing any state.
- Current-half progress resets from `[1, 2]` to `[0, 0]` on the tick the round overlay stops being drawn, while `FlowSnapshot.scores` stays `[0, 1]` and both loadouts stay `[[dazzle], [explosive-bullet]]` through the whole interval and into the bridge. The reset tick is bound to the adjacent pair PTS 2510656624/2510823290 (ticks 5493/5494), where the source drops `ROUND BLUE` and orange's large losing half circle in one 60 Hz step while blue's first completed-round HUD pip and both `Da`/`Ex` badges survive unchanged.
- The item catalog gains five definitions with the printed rules transcribed below and one distinct `art_key` each: `QUICK SHOT` is `Implemented`; `COLD BULLETS`, `CAREFUL PLANNING`, `OVERPOWER` and `BIG BULLET` are `CatalogOnly` and are rejected with the existing `UnimplementedItem` result when confirmed, without being removed from the offers, the fan or the hover path. The five are offered in the source order `QUICK SHOT, COLD BULLETS, CAREFUL PLANNING, OVERPOWER, BIG BULLET`; they do not join `general_implemented_offer_pool` for the rematch drafts, whose two five-card offers stay exactly as they are.
- Confirming `QUICK SHOT` appends it once to the drafting fighter's loadout, giving `[[dazzle, quick-shot], [explosive-bullet]]`, and its badge stack renders `Da Qu` while blue keeps `Ex`. A second confirmation, a duplicate of an item already in the loadout, or a confirmation of an item that was never hovered is rejected by the existing `Duplicate`/`NotHovered` policy and appends nothing.
- `QUICK SHOT`'s implemented capability is projectile speed and nothing else: it multiplies the owning fighter's own projectile launch speed by a typed per-fighter factor greater than one, leaves the shared `BULLET_SPEED` constant at 3,600 for every fighter and arena, adds no reload or ammunition lifecycle, and does not touch `fire_cooldown_extra_ticks`. The printed `+0.25s Reload time` is recorded in `docs/fidelity/footage-coverage.md` as a named unresolved gap owned by 048, not silently mapped onto the existing fire-cooldown seam. The factor's magnitude is an implementation choice, not a measurement: this interval contains no projectile at all, so a public regression proves only that the same fighter's shot travels measurably farther per tick after confirmation than before it, and no source-fitted number is claimed.
- All 73 published capture anchors keep the digests enumerated in the decision below. Concretely: the 36 anchors of the `teal-duel-replay` (5), `radial-saw-half-blue-replay` (8), `yellow-crate-terminal-blast-replay` (11) and `timber-collapse-replay` (12) profiles are byte-identical on all nine per-anchor digests, and the 37 `rematch-draft-replay` anchors are byte-identical on `frameSha256`, `combatSha256`, `roundSha256`, `arenaSha256`, `sawSha256` and `dynamicBodySha256`, with the moves in `stateSha256`, `flowSha256` and `loadoutSha256` enumerated per anchor before capture.
- `rounds-client capture-replay --profile rematch-draft-replay --seed 41 --ticks 5893` emits the existing 37 anchors plus eleven new ones at ticks 5466, 5478, 5479, 5493, 5494, 5588, 5710, 5801, 5802, 5818 and 5893, each carrying its native source PTS and RGBA SHA-256 in metadata, and each paired against its source frame at 1280×720. The three adjacent pairs are captured as pairs and reviewed as pairs.
- The full 5,893-tick route runs in the existing two-client smoke without raising `MAX_NETWORK_TICKS`: both separately launched UDP clients receive all 5,893 progressive snapshots, agree with the headless authority and the local client-host on the final state, flow and loadout digests, and observe the same draft phase order, active drafter, hovers, confirmation, half reset and retained completed rounds.
- The ordinary local keyboard and controller path drives this draft with the existing semantic `FlowCommand` values — arrows plus Enter/Space, D-pad plus south button — with no draft-specific key, no presentation-side item application and no new input mode, and `visible-flow --profile rematch-draft-replay --seed 41 --ticks 5893 --automated` completes the route and exits normally.
- `README.md`, `docs/architecture.md` and `docs/fidelity/footage-coverage.md` describe the post-round draft, publish the 5,893-tick commands, move the 04:10–04:20 row's draft from unresolved to implemented, and keep the hanging arena, the reload and ammunition lifecycle, `QUICK SHOT`'s source-fitted magnitude, the four catalog-only effects, later rounds, audio and production multiplayer named as gaps.

## Decisions

- Risk is 4 because the change reaches the serialised `FlowSnapshot` that every client, digest and capture anchor depends on: growing the embedded catalog and adding a typed speed capability necessarily moves three digests at all 37 rematch anchors, so a plausible implementation can be wrong in a way that is only visible as unexplained digest movement across the whole published evidence set rather than as a failing test.
- The source is `reference/MedalTVRounds20260903165304088.mp4`, SHA-256 `453954a7230401ed805be4e53dec41779a1913dfd69903671fc131fca2c8a18c`, re-hashed for this contract. The interval starts at ticket 046's endpoint PTS 2506156642 (clone tick 5466) and ends at PTS 2577323024 (clone tick 5893). The next frame, PTS 2577489690 / tick 5894, carries the incoming hanging body's first sliver and is out of scope, so the delivered route is 5,893 ticks and stops one tick short of the arena load. That leaves 107 ticks of headroom under `MAX_NETWORK_TICKS = 6,000`; no cap is raised and no replay profile is added.
- Identity is SHA-256 over exactly 3,686,400 native 1280×720 packed RGBA bytes with preserved integer PTS on the 1/10,000,000-second timebase, decoded through the retained imageio-ffmpeg FFmpeg 7.1 pipeline with `-copyts` and `-fps_mode passthrough`. PNG bytes and wall-clock seek labels are not identity.
- The eleven bound anchors, reproduced independently for this contract from a single decode of the whole interval and again from eleven single-frame decodes:

| Clone tick | PTS | Native RGBA SHA-256 | Direct observation |
|---:|---:|---|---|
| 5466 | 2506156642 | `ae4d4d943d9ec939064a0d9d4a3b08a8d6f31cb820639d297bf9caf1c5333a32` | Ticket 046's endpoint: `ROUND BLUE`, blue's first completed-round pip, orange's losing half circle, `Da`/`Ex`. |
| 5478 | 2508156634 | `15347b389c516246a5e0ce386c12d92ef52761643417424e49c8ea9fe8e91a12` | Round overlay alone; no card silhouette anywhere on the frame. |
| 5479 | 2508323300 | `3b393fadaa1c251dc11d1a4a618715ba8255bb2e111c5d26bfb38fa22a4b52b0` | A dark tilted card silhouette appears near native (150–230, 335–430), behind the still-present overlay and half circle. |
| 5493 | 2510656624 | `bacec49983693312b768ce1e5e2f7c86933b906f893cb2cd3c2abc8e56a5a020` | Last overlay frame: `ROUND BLUE`, orange's half circle, a raised face-up `QUICK SHOT` at left and two dark unrevealed cards. |
| 5494 | 2510823290 | `3334f88fc68abcaee4196a56e5cfbb2f2cfdd60995efeee43b18b564b826c8d8` | Adjacent frame: title and half circle both gone, orange fills the screen, two cards still unrevealed. HUD shows two five-slot rows with only blue's first slot filled. |
| 5588 | 2526489894 | `a8425d093602dc2d3732f7f5226bfe6cce51f5fe6259941ecfab8a97b09de65d` | All five faces legible; `OVERPOWER` raised with a red title; badges still `Da` and `Ex`. |
| 5710 | 2546823146 | `8d5c49166d275afbcd62d45c8a0724f5037bb70404645866bb0e2cf6738ae11a` | All five faces legible; `QUICK SHOT` raised with a gold title and gold corner accents; still no `Qu` badge. |
| 5801 | 2561989752 | `c2ee6e474627462c85dadd63e98e8adf00d92f93f2328bff4fe3c0121d35dbd0` | Orange's stack is `Da` alone; the raised `QUICK SHOT` is still only a hover. |
| 5802 | 2562156418 | `26aa07b3a28a66816e1d926c8459a5aead584ac0af5fd62ba90d58c194572d35` | Adjacent frame: the stack is `Da Qu`, with `Da` shifted left and `Qu` taking the right-aligned slot; blue keeps `Ex` and its pip. |
| 5818 | 2564823074 | `84d4c590074647e4195dcdc0c4a8e12ada5d7860ef665ab54f968aeffe162da5` | `QUICK SHOT` has lowered out of view; the other four faces remain raised and in place. |
| 5893 | 2577323024 | `ccc3b4919a7988b1e14e2566814a6c540b4f9f65e4b0f49c74d25b2ba6bfbb74` | Blue patterned background only: no arena, fighters or cards. Pips and `Da Qu`/`Ex` unchanged. |

- The three adjacent pairs bind three different events and must not be collapsed: 5478/5479 is the first card silhouette, 5493/5494 is the disappearance of the round overlay and the losing half, 5801/5802 is the first persistent `Qu` badge. The selected face leaves the fan at 5818, sixteen ticks after the badge appears, so the visible card motion is later than the authoritative selection.
- Pixels cannot say which frame of an adjacent pair carries the authoritative transition. This contract binds the authoritative draft-phase entry and the half reset to 5494, because that is the first frame on which the result title and the losing half are both absent, and treats the 5479 silhouette as a fifteen-tick presentation lead-in that rises behind a result the authority has not yet left. That split is an implementation choice, exactly as 048's admitted entry amendment separates arena-object presentation from held physics. The HUD never exposes a hidden half counter, so no reset tick is a proven source fact; a later contract that finds contrary evidence amends this rather than silently retuning it. Blocked ticket 048 chose the later hanging-arena handoff (tick 5894) for the same reset; whichever ticket lands second must reconcile the two boundaries rather than reset twice.
- The drafting-eligibility rule is worded for what one sample can carry. Orange drafts here, and orange is simultaneously the loser of the completed round, the player behind on completed rounds, and the loser of the last fight; a single observed draft cannot distinguish those three rules. This contract binds "the fighter who lost the completed round drafts", records the ambiguity in `docs/fidelity/first-loser-draft-observations.md` under this ticket's own section, and forbids any claim that the rule is source-proved. A test that only exercises the source colouring does not discharge this: the regression must also drive a route in which orange wins the round and prove that blue is then the sole drafter.
- The five faces are transcribed from the two legible frames at 5588 and 5710. Printed plus signs are transcribed as displayed even where the named property makes the trade undesirable, following the existing catalog convention:

| Card | Printed rules |
|---|---|
| `QUICK SHOT` | "A bunch more Bullet speed"; `+0.25s Reload time` |
| `COLD BULLETS` | "A bunch more Bullet slow"; `+0.25s Reload time` |
| `CAREFUL PLANNING` | "A bunch more DMG"; "Lower ATKSPD"; `+0.5s Reload time` |
| `OVERPOWER` | "Deal 15% of your max HP to enemies around you when you block"; "More HP"; `+0.25s Block cooldown` |
| `BIG BULLET` | "Bigger bullets"; `+0.25s Reload time` |

- Observed card colour, for the renderer and the `palette_rgb` column: the hovered `OVERPOWER` title samples about RGB (244, 78, 73) at 5588 and the hovered `QUICK SHOT` title about (222, 244, 102) at 5710, while both titles sample about (56, 52, 60) when not hovered. Cyan corner accents about (22, 165, 190) sit on `CAREFUL PLANNING` and `OVERPOWER` in both legible frames. `COLD BULLETS` and `BIG BULLET` are never hovered in this interval, so their highlight colours are unobserved and must be recorded as such rather than guessed into a rarity claim. Card art reads as: a gold blob with speed streaks, a bullet flanked by snowflakes, a blob behind a fan of papers, a red blob in dark glasses, and one oversized red round. Five distinct `art_key` values are required; the motifs are approximations of those readings, not extracted art.
- Only `Qu` is a source-observed badge abbreviation, from the 5802 frame. The other four two-letter forms are unobserved and may collide with existing values — the source's own prior stacks contain `Bu` and `Co` twice — so a collision is not a defect and must not be worked around with an invented disambiguator.
- The route's endpoint is the last background-only bridge frame. It is not a claim about how long the bridge lasts, and no hanging-arena geometry, suspension body, rope, anchor fixture, ammunition mark, reload arc, stored jump, wall launch, momentum absorption or projectile-scale recalibration enters this ticket. Those remain 048's, which stays blocked in `.ivy/worktrees/048-first-loser-draft/` on its suspension fit; this ticket takes only its physics-free prefix and changes nothing in that worktree.
- Adding five catalog entries necessarily changes `stateSha256` and `flowSha256` at every `rematch-draft-replay` anchor, because `FlowSnapshot.catalog` carries the whole `item_catalog()` and `hash_snapshot` serialises the entire `MatchSnapshot`; adding a typed speed field to `FighterCapabilities` and `GameplayModifiers` likewise changes `loadoutSha256`, since `loadout_digest` serialises `(loadouts, capabilities)`. The other four profiles report `flow: null` and are unaffected. The expected-identical set above is therefore the honest one, and it must be published before capture. Claiming all nine digests identical at all 73 anchors would be false; discovering the movement after capture and explaining it afterwards is not acceptable either.
- Extending the snapshot with new `FlowPhase` variants and a new capability field is a schema change: bump the sim snapshot protocol from 6 and the network protocol from 7 in the same change, and update the protocol numbers quoted in `docs/architecture.md`. Adding enum variants after the existing ones does not rename them, so `roundBlue` and every existing phase keep their serialised spelling.
- Give the post-round draft its own phases rather than reusing `Draft`/`Reveal`/`Handoff`. The rematch draft's cadence, two-drafter handoff and phase revisions run before tick 5466 and must not move; reusing those variants would couple this cadence to theirs and put the 37 existing anchors' `roundSha256` at risk for no gain.
- Reuse the existing authority, ECS world, private Rapier boundary, `rematch-draft-replay` profile, UDP adapter, shared Bevy scene and capture path. No new replay profile, renderer, fullscreen pass, evidence launcher, input mode, item framework or transport claim. The source-shaped hover and confirmation ticks are configuration on the existing semantic command boundary; live input must be able to hover and confirm any of the five offers, and to confirm none of them, at any tick the draft phase accepts commands.
- The recording drops three frames inside this interval: the decode returns 426 frames where the 60 Hz PTS lattice has 429 slots, with one-frame gaps at 2522989908→2523323240, 2541989832→2542323164 and 2566323068→2566656400. The tick mapping above is derived from the PTS lattice, not from counting decoded frames, and no anchor falls in a gap. An implementer who maps ticks by frame index will be three ticks wrong by the end of the interval.
- This bounded child advances umbrellas 018, 020 and 025 without closing or changing their contracts. Cargo must use `.cargo/config.toml`: at most two concurrent jobs, the reusable ignored target at `out/cargo-target`, no upward job-cap override and no concurrent builds; reuse that target across test, lint, build, capture and review. Every visible window must open on monitor 4, the 1920×1080 display at zero-based screen index 3, launched hidden or minimized and verified at that position before being shown.

## Evidence required

- Re-hash the recording, then reproduce all eleven native RGBA identities above from a decode of the interval and retain the decoder command arrays, the per-frame PTS/hash record and any mismatch. Retain the three-frame drop finding with the gap PTS values so the tick mapping stays auditable.
- One connected public-input regression continues the 046 connected regression through this draft on ordinary inputs: it reaches `RoundBlue` from real combat, leaves it into the post-round draft with the loser active, observes the five offers in source order, accepts both hovers, confirms `QUICK SHOT`, and ends in the bridge. It asserts the loadout append (`[[dazzle, quick-shot], [explosive-bullet]]`), the half reset to `[0, 0]` on the bound tick, retained completed rounds `[0, 1]`, and retained `Da`/`Ex` behind the new `Qu`. Negative cases in the same path: the winner's hover and confirm are `WrongPlayer`; a stale `phase_revision` is `Stale`; each of the four catalog-only items is `UnimplementedItem` on confirm while still hovering successfully; a repeat confirm is `Duplicate`; a confirm without a matching hover is `NotHovered`. Every rejection leaves halves, rounds, loadouts and capabilities unchanged.
- A second route in the same regression gives orange the round instead, and proves blue is then the sole drafter with orange's loadout untouched, so the eligibility rule is exercised in both colours rather than only in the source colouring.
- A compact weapon regression through the public input path fires the same fighter before and after confirming `QUICK SHOT` from identical positions and aim, and proves the post-draft projectile advances measurably farther per tick while the opposing fighter's projectile is unchanged and the shared `BULLET_SPEED` constant is untouched. It must not assert a source-fitted magnitude, and it must show that no reload, ammunition or fire-cooldown state changed.
- Capture the eleven new shared-renderer anchors and pair each against its bound source frame at 1280×720, reviewing the three adjacent pairs as pairs: the silhouette appearing, the overlay and losing half disappearing together while blue's pip survives, and `Da` becoming `Da Qu`. Record the remaining visible differences concretely — fan curvature, face construction, card art, type weight, easing — in a new section of `docs/fidelity/first-loser-draft-observations.md` under this ticket, rather than behind an equivalence claim.
- Publish the expected-identical anchor set before capture, then recapture all 73 anchors at their published seeds and tick counts (teal 38/786, radial 42/938, yellow 43/155, timber 40/1440, rematch 41/5893) and compare against the retained baseline. The 36 non-rematch anchors must match on all nine digests. The 37 rematch anchors must match on `frameSha256`, `combatSha256`, `roundSha256`, `arenaSha256`, `sawSha256` and `dynamicBodySha256`; every `stateSha256`, `flowSha256` and `loadoutSha256` move must appear in the published list with its cause. `inputTraceSha256` and `executableSha256` move for the usual reasons. Any other movement is a defect to diagnose, not to re-baseline.
- Run the complete two-client smoke over the 5,893-tick route and record every handshake, monotonic input sequence, progressive snapshot count, and agreement among both clients, the headless server and the local client-host, including the flow and loadout digests and one received-state GPU frame bound to the agreed final state. Confirm the 6,001-tick rejection still holds and that both processes exit and are cleaned up.
- Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo build --workspace --locked`, `cargo test --workspace --locked`, the connected checks, bounded offscreen capture and the bounded automated visible flow against the capped reusable target, plus `git diff --check`. Verify the window's centre is inside the monitor-4 rectangle before it is shown, and say plainly if a physical controller was not operated.
- Keep a short responsibility inventory showing that the draft adds playable flow and presentation behaviour at existing boundaries, with no second authority, renderer, launcher or transport, and with test and support growth proportionate to the behaviour it protects.

## Work log

- 2026-09-07T17:31:46Z stage research start session claude:96849848-e6ec-488e-b4ac-b113acc49f8a/shape-053-opus — Re-hashing the recording and decoding PTS 2506156642–2577489690 to reproduce ticket 048's eleven bound anchors independently before shaping anything.
- 2026-09-07T17:39:08Z stage research end session claude:96849848-e6ec-488e-b4ac-b113acc49f8a/shape-053-opus — Recording hash reproduced; all eleven anchors plus the excluded 2577489690 frame reproduced twice, from a whole-interval decode and from single-frame decodes, with no mismatch. Found three dropped source frames inside the interval (426 decoded against 429 lattice slots). Public inspect at seven tick counts shows phase `roundBlue` at revision 22 and an unchanged combat digest and fighter state from 5466 through 5894, so no fighter physics runs in the interval and `RoundBlue` has no exit. All 73 published anchors are at or before tick 5466. Commands, hashes and frames are under `out/ticket-053/`.
- 2026-09-07T17:39:20Z stage design start session claude:96849848-e6ec-488e-b4ac-b113acc49f8a/shape-053-opus — Shaping the physics-free prefix of blocked ticket 048 into a flow-and-presentation contract bound to the eleven reproduced anchors.
- 2026-09-07T17:46:30Z stage design end session claude:96849848-e6ec-488e-b4ac-b113acc49f8a/shape-053-opus — Contract written against the eleven reproduced anchors, the transcribed five faces and the inspect trace. Two findings changed the shape it was asked for: the recording drops three frames inside the interval, so tick mapping must come from the PTS lattice; and `FlowSnapshot.catalog` plus `FighterCapabilities` are inside the hashed snapshot, so `stateSha256`, `flowSha256` and `loadoutSha256` cannot stay identical at the 37 rematch anchors. The expected-identical set records what is actually preservable. No product code, test, existing doc or ticket 052 was touched; no Cargo ran and no window opened.
