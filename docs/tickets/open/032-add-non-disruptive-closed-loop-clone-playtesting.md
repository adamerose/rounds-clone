---
format: 3
status: ready
created: 2026-08-29T20:17:24Z
origin: human-request
tags: ["verification", "playtesting", "infrastructure", "product-fidelity"]
value: 9
risk: 4
sessions:
  - codex:01a0492f-57bc-7f32-87fa-1fbe5d483893
execution: unattended
depends-on: [15, 30]
supersedes: []
split-from: [22]
---

# Add non-disruptive closed-loop clone playtesting

The clone can replay predetermined inputs and render selected states, but an agent cannot yet observe the running game, choose legal actions from those observations, and continue playing without controlling the user's desktop. Add a debug-only closed-loop playtest surface so visual and behavioral mismatches can be explored during development without moving the physical pointer, injecting global keyboard input, activating a foreground window, or exposing an invented player-facing bot mode.

## Outcome

- A distinct `--debug-agent-playtest <absolute-output-root>` startup mode accepts versioned legal semantic actions for both players, advances the real deterministic match shell, and returns one ordered observation after each accepted action interval without using operating-system mouse, keyboard, controller, focus, or window-activation APIs.
- Human-style decision observations contain only a monotonically increasing sequence number and the renderer-owned frame a person would see. Seed, exact tick, accepted interval, per-tick match hashes, and protocol diagnostics are written by a separate trace recorder and are not present in the driver callback or decision-response type; a separately named structural mode may expose them only with a `non-human-structural` trace label and cannot satisfy perceptual evidence.
- Starting from `Match.Create(1UL)`, a local deterministic adaptive driver controls both players through the two opening picks and first full round, then stops successfully only when `FaithfulSubsetMatchShell.IsAtIncompleteFidelityBoundary` becomes true at the first loser-draft boundary and the complete incomplete-fidelity message is rendered. Reaching `MatchResult`, exhausting a bound, EOF, or quitting earlier is failure.
- At least one bounded renderer-backed run uses multiple returned frames to choose subsequent legal actions and reaches that exact terminal boundary. An explicit renderer-unavailable result is valid for headless protocol tests but cannot close the ticket or satisfy human-style playtest evidence.
- The completed debug trace replays the same accepted interval actions into a fresh shell and reproduces every stored `Match.Hash` value and the same terminal boundary. It is debug evidence only, not a new protected replay, golden fixture, or complete-match replay format.
- The route enforces the exact resource, artifact, timeout, and cleanup limits below, exits automatically, refuses evidence overwrite, and leaves no attributable process. It remains internal verification infrastructure and is unreachable from ordinary play, release routing, the existing incomplete-fidelity capture route, and protected replay routing.

## Decisions

- The protocol identifier is `rounds-agent-playtest-v1`. The standard-input/output transport processes one complete UTF-8 JSON request record at a time after the initial sequence-0 observation; duplicate, skipped, negative, or out-of-order sequence numbers fail before advancing simulation. Deterministic standard streams cannot prove the physical time at which an external writer sent otherwise valid bytes, so pipe-peek timing is not an evidence boundary. Instead, the owner-mediated acceptance driver receives and decodes one finalized frame, calls `Choose(HumanPlaytestObservation)` exactly once, writes only that returned request, and reads its one response before the next callback. Runs driven by preloaded or otherwise non-causal input may exercise protocol parsing but cannot satisfy adaptive renderer evidence or publish `complete:true`.
- Each request is `{protocol, sequence, intervalTicks, players}`. `intervalTicks` is an integer from 1 through 30. `players` has exactly two entries, each containing `move` in `-1|0|1`, Boolean `jump`, `fire`, and `block`, plus finite `aimX` and `aimY` in `[-1,1]`; unknown or missing fields, non-finite numbers, or extra/missing players fail closed.
- Transport is newline-delimited UTF-8 JSON: requests use standard input and protocol responses use standard output. The launcher uses Godot's quiet mode, all diagnostics and engine logging go to standard error, each protocol object occupies exactly one LF-terminated line, and no other standard-output record is permitted.
- Initial sequence 0 and every accepted request produce exactly one frame response: `{protocol:"rounds-agent-playtest-v1",sequence:N,status:"frame",framePath:<absolute finalized PNG>,frameSha256:<lowercase 64-hex>,width:W,height:H,terminal:B}` with no additional fields. The route writes and closes `frame-NNNN.png.partial`, verifies dimensions and SHA-256, atomically renames it without overwrite to `frame-NNNN.png`, and only then publishes the response; `terminal` is true only for the rendered first-loser-draft incomplete-fidelity boundary.
- Any failure produces exactly one response with no frame fields: `{protocol:"rounds-agent-playtest-v1",sequence:N-or-null,status:"error",stage:S,code:C}`. Allowed stage/code pairs are `request-parse/malformed-json`, `request-validate/invalid-schema`, `sequence/invalid-sequence`, `simulation/simulation-failed`, `renderer/renderer-unavailable`, `frame/frame-publish-failed`, `resource/resource-limit-exceeded`, `lifecycle/timeout`, `lifecycle/cleanup-failed`, `terminal/invalid-terminal`, and `replay/replay-mismatch`; the process then exits nonzero after cleanup. `sequence` echoes a valid parsed integer and is null otherwise. An invalid request never advances or produces a frame.
- Each action is held unchanged for every 60 Hz tick in its interval. Draft movement, draft confirmation, jump, and block preserve the simulation's existing edge semantics: a new edge requires at least one intervening tick with the relevant move axis at zero or held Boolean false. A zero aim vector preserves the current aim; any nonzero finite vector follows the existing normalization path. Simultaneous legal controls remain legal.
- The owner verifies the finalized response path is the exact expected file beneath its task-owned root, rechecks its declared hash and dimensions, decodes it completely, and passes the local adaptive driver an immutable `HumanPlaytestObservation(sequence, pixelBytes, width, height)` value only. `pixelBytes` is an immutable copied buffer of tightly packed rows in top-to-bottom order, four bytes per pixel in red-green-blue-alpha order, eight bits per channel, sRGB color bytes, and straight unpremultiplied alpha, with no padding (`pixelBytes.Length == width * height * 4`) or mutable decoder backing exposed. It never passes the path, hash, terminal flag, trace writer, or structural observation into that callback. Trace-envelope metadata is passed directly from the owner to a separate recorder; direct type/API tests prove the adaptive driver cannot read it. Structural mode uses a distinct request flag and `non-human-structural` label and is excluded from renderer-backed acceptance.
- Model reasoning, when used later, is an external low-frequency supervisor over this protocol. This ticket adds no SDK, outbound request, API key, account, spend, model selection, or nondeterministic dependency; admission evidence uses a local deterministic adaptive driver and a counterfactual test in which a different visible observation with identical prior history causes a different legal next action.
- The route owns at most 120 action requests, 3,600 simulation ticks, 121 rendered frames including sequence 0, 90 seconds wall time, two players, and the integrated `CombatTuning.LiveBulletCap` of 2,048 live bullets. Crossing any limit fails and triggers owner cleanup.
- Renderer-backed output is at most 1280 by 720 and one frame per observation, with no more than 10 observations emitted per wall-clock second. The complete task-owned process tree contains only the driver/owner and game, runs below normal priority on at most two logical processors, stays below two GiB private memory and two GiB attributable dedicated GPU memory, and rejects a run that exceeds 70 percent total GPU-engine utilization.
- Before a renderer-backed run, collect ticket 031's independent 10-second, 20-millisecond scheduling-heartbeat baseline. During the run, the 95th-percentile delay must be no more than 5 milliseconds above baseline and no more than 15 milliseconds absolute, with no delay above 50 milliseconds; otherwise kill the exact task-owned process tree and reject the evidence. No renderer-backed or GPU proof runs while the user is active unless these gates and automatic cleanup are already in force; pure simulation/protocol tests may proceed. The in-route 90-second deadline also governs an incomplete or unterminated request read: expiration cancels or abandons the pending read, cleans the owned output, emits exactly one `lifecycle/timeout` error, and exits without waiting for LF or EOF; the owner's 95-second kill remains an independent outer bound.
- Use only the standard-input/standard-output NDJSON boundary defined above; do not open a local pipe or network listener, install a virtual input driver, enable a Windows feature, create an account, or change host security configuration. The owner applies a 95-second hard timeout and exact process-tree cleanup in addition to the in-route 90-second limit.
- The output root is normalized before routing, must be an absent absolute child of an existing non-reparse directory, and must be created exclusively; a trailing separator cannot change the sibling-lock location, and routing rejects an absent parent rather than implicitly creating or owning a parent chain. It contains at most `frame-0000.png` through `frame-0120.png`, each created without overwrite, plus `trace.jsonl.partial` while running; successful replay verification atomically renames that trace to `trace.jsonl` and atomically publishes `manifest.json` with protocol/build identity, dimensions, frame hashes, accepted actions, tick/hash coverage, limits, samples, terminal boundary, and completion marker. Total output is capped at 256 MiB.
- Malformed input, timeout, crash, resource violation, renderer error, or incomplete replay verification produces no final manifest: the owner kills only the exact task-owned process tree, removes the exact task-owned output root including partial frames/trace, emits one stage-specific error marker, and proves both path and process absence. Cleanup keeps the sibling ownership lease until exact deletion succeeds, performs bounded retries while that lease remains held, and never emits the original error before cleanup completes. If deletion still fails, the sole response is `lifecycle/cleanup-failed`, the process exits nonzero, and the retained residue is not represented as successful cleanup. Raw frames remain external and untracked; committed evidence contains only hashes, dimensions, tick identities, semantic traces when clean-room-safe, resource samples, and independently derived findings.
- This ticket does not claim fidelity to installed ROUNDS, replace ticket 031's target-build capture, or deliver ticket 022's complete-match replay and 10,000-match self-play. It supplies the smaller clone-side interaction surface those later comparisons can use.
- `StartupMode.DebugAgentPlaytest` is mutually exclusive with ordinary match, replay, and the existing incomplete-fidelity evidence mode; parsing requires a debug build and a valid absent absolute output root before any protocol/file creation. Its owner disables `_PhysicsProcess` and advances only supplied `PlayerInput` values; ordinary match `_PhysicsProcess` remains the only route that reads `Godot.Input` or mouse position, and tests prove the playtest route never enters it.
- No visible verification window is allowed while the user is active. The required renderer-backed proof must be deferred if it cannot remain non-activating and wholly on monitor 4 without weakening any input, placement, or host-impact gate.

## Evidence required

- Focused protocol tests prove the exact NDJSON request/frame/error schemas and framing, atomic final-frame-before-response ordering, canonical immutable top-to-bottom tightly packed straight-alpha sRGB RGBA8 callback bytes and length, standard-error diagnostic separation, owner-mediated frame-to-action callback causality, generic one-record-at-a-time processing without a pipe-timing claim, held and rising-edge semantics, legal simultaneous controls, 1–30-tick intervals, 120-request/3,600-tick/2,048-bullet bounds, trace hashes, fresh-shell replay, terminal boundary, every allowed error pair including `lifecycle/cleanup-failed`, malformed/duplicate/out-of-order rejection, incomplete-line 90-second timeout, normalized-root routing, bounded cleanup retry, artifact lifecycle, and exact cleanup.
- Negative route tests prove ordinary, release, existing incomplete-fidelity evidence, and protected replay routes cannot enable the playtest surface; invalid/existing/relative output roots create nothing; missing priority/affinity/resource/heartbeat limits, OS-level input fallback, and renderer unavailability fail explicitly. Direct code checks keep `Godot.Input` and global mouse reads exclusively in ordinary match processing.
- Two independent runs of the same accepted action trace produce identical actions, every per-tick `Match.Hash`, phase transitions, incomplete-fidelity terminal boundary, trace/manifest bytes apart from explicitly listed timing/resource samples, and renderer-frame hashes under the same rendering configuration.
- A bounded renderer-backed run wholly on monitor 4 supplies at least five distinct frames, including sequence 0, active duel play, and the terminal incomplete-fidelity frame, to the local adaptive driver's human-only callback. A counterfactual visible frame produces a different legal action, and the report separates visual findings, behavioral findings, resource/heartbeat evidence, unavailable diagnostics, and exact cleanup.
- Process telemetry proves the CPU-affinity, priority, process-count, memory, GPU, cadence, resolution, output-size, and responsiveness bounds throughout the renderer-backed run. The exact target window is non-activating with its center on monitor 4 before every captured frame; no window, input, or unrelated pixels from monitors 1 through 3 are observed.
- Zero-warning build, focused and full simulation/replay suites, repository checks, ticket checker, and `git diff --check` pass. Existing golden replay bytes, ordinary-play behavior, dependencies, and shipped UI remain unchanged.

## Work log

- 2026-08-29T20:17:24Z stage design start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Split a small clone-side closed-loop playtest surface from later complete-match self-play after the user asked for interactive model testing without interfering with their mouse or primary monitor.
- 2026-08-29T20:18:10Z stage design end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Bound the debug surface to semantic in-engine actions, low-frequency observation/action turns, deterministic trace replay, no global input or activation, explicit human-versus-structural observations, resource limits, release refusals, and no claim of target-build fidelity or complete-match self-play.
- 2026-08-29T20:21:01Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Cold-read the proposed clone playtest contract against the integrated routes, semantic inputs, shell boundary, replay ownership, and the user's no-input/no-lag requirement.
- 2026-08-29T20:21:01Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Rejected risk-4 admission because human observations leaked structural metadata, visual evidence was optional, resource/artifact/protocol/start/terminal/replay/model boundaries remained under-specified, and debug isolation was not yet proven.
- 2026-08-29T20:23:32Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Resolving all admission findings with separate decision and trace channels, mandatory visual closure, executable host limits, an exact protocol and supported-shell terminal, debug-only routing, atomic artifact cleanup, deterministic local adaptivity, and durable handoff to complete-match self-play.
- 2026-08-29T20:24:47Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Defined the exact v1 action/edge/sequence protocol, two-channel observation model, seed-1 supported-shell boundary, trace replay ownership, mandatory five-frame adaptive proof, process/GPU/memory/heartbeat limits, create-new atomic artifact lifecycle, and debug/release isolation without adding a provider or network dependency.
- 2026-08-29T20:25:15Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Re-reading the corrected contract and shaped ticket-022 dependency against all nine prior findings.
- 2026-08-29T20:26:32Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Rejected with one remaining finding: requests were exact, but the standard-stream or pipe response framing, atomic frame representation, error grammar, and human callback boundary still required implementer choices; all other findings and ticket 022's ownership handoff passed.
- 2026-08-29T20:27:15Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Choosing one exact NDJSON standard-input/output transport with finalized-frame responses, enumerated frame-free errors, stderr-only diagnostics, atomic PNG publication, and a verified immutable-pixel callback.
- 2026-08-29T20:27:56Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Bound every request to one LF-delimited standard-output frame or enumerated error response, required final atomic PNG publication before response, kept diagnostics on stderr, and limited the adaptive callback to verified immutable pixels with no trace metadata.
- 2026-08-29T20:28:15Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Verifying the selected transport and callback representation while regressing every previously resolved boundary.
- 2026-08-29T20:28:43Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Rejected two narrow contradictions: a stale local-pipe alternative reopened transport choice, and decoded pixel row/channel/alpha/stride/color semantics remained unspecified; all other contract and dependency findings passed at risk 4.
- 2026-08-29T20:29:03Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Removing the stale pipe alternative and fixing the immutable callback to one exact tightly packed top-to-bottom straight-alpha sRGB RGBA8 representation.
- 2026-08-29T20:29:38Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Closed the final transport ambiguity by forbidding pipes and defined callback rows, channel order, bit depth, color bytes, straight alpha, stride, exact length, copying, and immutability.
- 2026-08-29T20:29:57Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Final cold read of the exact transport, callback, visual, resource, lifecycle, replay, provider, route-isolation, and ticket-022 ownership boundaries.
- 2026-08-29T20:30:04Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f2d-3443-7651-a2a4-504a996acf8d — Admitted at risk 4 with no findings; ticket 022's dependency and reviewed-migration handoff are coherent and acyclic, and no GUI, game, input, or GPU-heavy work was used for admission.
- 2026-08-29T20:42:39Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Implementing the frozen headless-safe portion while the user is active: debug-route isolation, strict NDJSON, semantic interval stepping, separate human and structural observations, immutable RGBA8 boundaries, deterministic trace replay, artifact ownership, resource refusals, and focused tests; renderer-backed proof remains explicitly deferred.
- 2026-08-29T20:47:28Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Completed the headless-safe protocol core and verified a zero-warning game build, 30 focused route/protocol tests, 234 applicable simulation tests, repository checks, protected-artifact non-drift, and whitespace; no Godot runtime, renderer, window, browser, video/GPU workload, pixel inspection, or operating-system input was used, so the mandatory real renderer adapter, five-frame adaptive run, telemetry, monitor attestation, and independently repeated frame hashes remain undelivered and the ticket stays open.
- 2026-08-29T20:49:21.975Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f48-b924-76f1-94e9-6512e892475d — Cold-reviewing exact candidate range `647416b3dd7503cf94199b79de8756ddc025c7c0..e75d564152b293214371c86681a1d1c101077c36` against the admitted protocol, terminal, trace, artifact-ownership, manifest, and resource contracts.
- 2026-08-29T20:57:43.955Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f48-b924-76f1-94e9-6512e892475d — Rejected with five findings: terminal requests recorded phantom tail ticks, preloaded input bypassed one-request/one-response, finalized traces were neither strictly parsed nor replayed and the manifest claimed unsupported completion, output-root acquisition had a delete race, and negative heartbeat measurements passed.
- 2026-08-29T20:57:44Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Correcting all five findings headlessly while preserving explicit renderer unavailability and the open-ticket boundary.
- 2026-08-29T21:15:53Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Resolved all five findings: terminal consumption now stops at real tick 496 with requested-versus-accepted intervals, a turnwise transport rejects queued requests before they advance, finalized canonical traces stay read-locked while they are parsed and replayed before a validated test-only non-evidence manifest, atomic Windows root acquisition preserves adversarial content and gates recursive cleanup on a live ownership lease, and negative heartbeat measurements fail; 35 focused and 240 applicable tests, zero-warning builds, repository checks, protected-artifact non-drift, whitespace, and future temporary-path cleanup passed without running any renderer or GUI evidence.
- 2026-08-29T21:19:34Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Continuing parent-orchestrator static hardening on two residual races: make real redirected standard input usable through a bounded inherited-pipe pending-byte probe while unsupported handles refuse before stepping, and retain the exclusive sibling ownership lock through exact failed-run deletion.
- 2026-08-29T21:28:41.167Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Added a verified inherited-standard-input Windows pipe probe for empty, complete, malformed, partial, and unsupported pending states, re-probed immediately before each response, refused unsupported handles before simulation, and moved recursive cleanup behind a lease operation that holds the exclusive sibling lock until deletion finishes; 37 focused and 242 applicable tests, zero-warning builds, repository checks, protected-artifact non-drift, and whitespace passed headlessly while renderer evidence remains unavailable.
- 2026-08-29T21:30:02.747Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f48-b924-76f1-94e9-6512e892475d — Cold-reviewing final-ancestry range `d213d5b3053971c0b6e44d2a66f48745af11eb6b..479df8c3dd7048a64a13f13189cf6e565f7aef84` against the prior findings and the parent hardening.
- 2026-08-29T21:37:43.755Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f48-b924-76f1-94e9-6512e892475d — Rejected physical pipe-wait attestation as a remaining time-of-check/time-of-use claim, an unterminated request that could outlive the 90-second route deadline, non-retryable cleanup exceptions that could duplicate or suppress errors, and output roots whose accepted parent/trailing-separator shapes could not be acquired safely.
- 2026-08-29T21:38:45.7093584Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Reframing closed-loop evidence around owner-mediated verified-frame-to-action causality rather than impossible deterministic pipe timing, while making request timeout, sole-error cleanup failure, and normalized-root acquisition exact.
- 2026-08-29T21:40:11.4628202Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f3a-8b6c-7471-91ea-749dd8a0df5b — Cold-reading the corrected risk-4 contract for causal owner mediation, bounded unterminated reads, sole cleanup-failed responses, retained cleanup ownership, and normalized existing-parent acquisition.
- 2026-08-29T21:42:44.8677099Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f3a-8b6c-7471-91ea-749dd8a0df5b — Admitted at risk 4 with no findings; the causal receipt, timeout, cleanup retry, sole-error, and normalized-root boundaries are complete and mechanically implementable.
- 2026-08-29T21:44:30.8612100Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Implementing the re-admitted contract headlessly: owner-mediated causal driving, deadline-bounded NDJSON reads, cleanup-failed sole responses with retained lease retries, and normalized existing-parent root acquisition while renderer evidence stays unavailable.
- 2026-08-29T21:59:50.7298177Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Replaced physical pipe timing with a concrete verified-frame/immutable-observation/choose/write/read owner supervisor and receipt-gated completion, bounded incomplete NDJSON reads by the remaining route deadline, made cleanup precede one sole error with lease-held retries and honest cleanup-failed residue, and unified normalized absent-child acquisition under an existing non-reparse parent; 41 focused and 246 applicable tests, zero-warning builds, repository checks, protected-artifact non-drift, and whitespace passed headlessly while real renderer, telemetry, monitor, and repeated-frame evidence remain undelivered.
- 2026-08-29T22:02:07.349Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f48-b924-76f1-94e9-6512e892475d — Cold-reviewing exact candidate `b0befec501c91266086dcb9bace6bbbb53abed12` against the re-admitted causal-proof, terminal-observation, artifact-cap, and stable-parent acquisition contract.
- 2026-08-29T22:11:47.322Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a04f48-b924-76f1-94e9-6512e892475d — Rejected with four findings: caller-authored causal receipts could forge completion without the supervisor, the terminal finalized frame bypassed verification and the human callback, artifact bytes crossed the 256 MiB cap before rejection, and parent path validation could race replacement before sibling-lock/root creation.
- 2026-08-29T22:11:47.323Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Correcting all four findings without weakening the re-admitted contract or claiming unavailable renderer, telemetry, monitor, or repeated-frame evidence.
- 2026-08-29T22:26:13.459Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Replaced caller-authored receipts with a supervisor-issued single-use proof bound to verified ordered frames, actions, and the observed terminal frame; terminal responses now verify and reach the human-only observer without choosing or writing; frame, trace, and manifest bytes are preflight-capped; and acquisition holds and revalidates a non-reparse parent file identity while preserving adversarial replacements; 43 focused and 248 applicable tests, zero-warning builds, repository checks, protected-artifact non-drift, and whitespace passed headlessly while mandatory renderer evidence remains undelivered and completion remains false.
