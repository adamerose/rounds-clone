---
format: 3
status: ready
created: 2026-08-30T03:14:10Z
origin: agent-proposed
tags: ["verification", "projectiles", "playtesting"]
value: 7
risk: 4
sessions:
  - codex:01a0492f-57bc-7f32-87fa-1fbe5d483893
execution: unattended
depends-on: [15, 30, 33]
supersedes: []
split-from: [32, 34]
---

# Add a deterministic base-projectile evidence route and isolated launcher

Ticket 034's source correction cannot be visually approved without rendering an exact-candidate projectile, but reaching that transient state through interactive play would require operating-system input and could disrupt the user.
The deterministic route now exists, but a normal Godot launch could still activate on the user's interactive desktop, start before resource controls are applied, publish into an ambiguously owned path, or render stale assemblies.
Complete the route and add a bounded Windows launcher that can later render the exact candidate on a private desktop without ever switching that desktop into the input path.

## Outcome

- A debug-only command-line route constructs an active `arena-006` world through the real simulation API and vanilla player/combat profiles; the frozen state contains exactly one ordinary owner-0 projectile in flight, with no cards or custom combat profile involved.
- The route draws one frame, publishes `frame-0000.png` beneath one atomically acquired, absent absolute output root, emits one exact projectile-specific attestation, and exits automatically.
- A Windows-only launcher has a side-effect-free plan mode and one explicitly separate execute mode. Execute mode creates a private desktop, never calls `SwitchDesktop`, and starts the pinned Godot process there suspended and contained in a kill-on-close job before it can run.
- The launcher fails closed unless the preflight topology identifies `\\.\DISPLAY4` at Godot screen index 3 with physical bounds `(364,-1080) 1920x1080`, the intended 1280x720 window lies wholly within those bounds, and the process attests the same private desktop, screen, position, and size.
- A successful run is attributable to one clean candidate commit, one immediately preceding bounded build, one exact runtime assembly hash/MVID, one exact pinned Godot executable, one owned output root, one validated PNG, and one child process tree that has fully terminated.
- Ordinary play, replay, the incomplete-fidelity boundary capture, and agent playtesting retain their existing routing and behavior.
- The route never reads global input and never enters continuous physics.

## Decisions

- Reuse `DebugEvidenceCaptureProtocol` and the existing viewport/monitor attestation lifecycle rather than adding another capture implementation.
- Give projectile evidence its own exact completion/error marker namespace; preserve every byte of the existing incomplete-fidelity evidence markers.
- Reach the projectile through `World.CreateMatch` and `Sim.Step`, using embedded `arena-006`, `PlayerTuning.Vanilla`, `CombatTuning.Vanilla`, and two `PlayerCombatProfile.Vanilla` instances.
- Fire upward from player 0 on the first active tick so the projectile is visible, remains clear of the arena geometry, and is produced by ordinary fire mechanics.
- Replace the projectile route's bare PNG destination with the existing reparse-safe `AgentPlaytestArtifactOwner` absent-root acquisition and finalized-frame verification. Add the narrow Godot PNG decoder adapter needed to publish one RGBA8 frame and a `CompleteVerifiedSingleFrame` transition that succeeds only after exactly terminal sequence 0 has been published and reverified, marks the owner complete without a manifest, and rejects missing, nonterminal, repeated, or extra frames. Do not otherwise weaken or fork the ownership rules.
- Set per-monitor-v2 DPI awareness before topology enumeration. Identify the monitor by the exact Win32 device name `\\.\DISPLAY4`, use physical-pixel bounds throughout, require its enumerated ordinal to be 3, and pass both canonical `--screen 3` and `--position 684,-900` pre-start arguments. Any topology, DPI, ordinal, or containment mismatch is a refusal, not a fallback.
- The executable is the repository's bootstrap-pinned Godot 4.7.1 .NET console binary at `.tools/godot-4.7.1/Godot_v4.7.1-stable_mono_win64/Godot_v4.7.1-stable_mono_win64_console.exe`, version `4.7.1.stable.mono.official`, SHA-256 `b2c334ff6bf1e07ded41b80bd6f4785485650db6ddbb2740b802930f35237c26`. Resolve this exact path beneath the candidate repository and refuse any reparse point, version, or hash mismatch.
- Build the exact clean candidate commit immediately before execute mode with `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\MSBuild\Current\Bin\MSBuild.exe game\Rounds.Game.csproj /noAutoResponse /t:Rebuild /p:Configuration=Debug /p:Restore=false /p:UseSharedCompilation=false /p:BuildProjectReferences=true /m:1 /nr:false /v:minimal /warnaserror`. `/noAutoResponse` must disable both executable-adjacent `MSBuild.rsp` and upward-searched `Directory.Build.rsp`. Require file version `17.14.40.60911`, product version `17.14.40+3e744208875e56e4bf0bc22c40a1c431fb150987`, and executable SHA-256 `0f7daba445ba37b652ae4180270ab3eff27acbbe5d411d3b05dad51aab16404c`. Use an explicit non-inherited sanitized environment containing only reviewed OS necessities plus pinned build entries; require exact effective values and prohibit ambient MSBuild, NuGet, SDK, restore, and DOTNET injection. Set `DOTNET_PROCESSOR_COUNT=2`, `MSBUILDDISABLENODEREUSE=1`, `MSBuildEnableWorkloadResolver=false`, and `MSBuildSDKsPath=<repository>/.tools/dotnet/sdk/8.0.423/Sdks`; require the exact SDK version and bytes from `global.json` with roll-forward disabled, exact reference pack 8.0.29, identity-bound project/lock/assets inputs for Game/Replay/Sim, and exact identity-bound Godot package cache content. Refuse missing or mismatched locked inputs without restore or network. The direct child starts suspended, matches the retained opened MSBuild identity before resume, runs in a separate kill-on-close job at affinity `0x3`, below-normal priority, one-process and admitted memory limits, drains bounded stdout/stderr concurrently, and must prove zero exit, zero warnings, and empty job by the deadline. `Rebuild` must prove the prior runtime path absent before spawn, recreate and retain no-write/no-delete identity leases for `Rounds.Game.dll`, `Rounds.Replay.dll`, and `Rounds.Sim.dll`, hash/read exact stable PE/MVID/assembly-name data only through those handles, and retain their reparse-free ancestor chains through preflight, Godot load, child attestation, zero exit, and empty-job proof. The child computes and attests its own Game assembly hash/MVID and the launcher requires equality.
- Use direct `CreateProcessW` with an argv-to-command-line encoder tested against Windows parsing, `CREATE_SUSPENDED | CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP | BELOW_NORMAL_PRIORITY_CLASS | EXTENDED_STARTUPINFO_PRESENT | CREATE_UNICODE_ENVIRONMENT`, an explicit Unicode environment block, and `STARTUPINFOEXW.lpDesktop` naming a newly created nonce-scoped private desktop. Its `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` contains exactly four inheritable child handles: a read-only `NUL` standard input, the stdout pipe write end, the stderr pipe write end, and the acknowledgement pipe read end; every parent-side handle and every other launcher handle is non-inheritable. Do not use a shell, `Process.Start`, a script-spawned child, or an untested string command API.
- Before `ResumeThread`, create a Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, a single-active-process limit, below-normal priority, a two-CPU affinity mask (`0x3`), a 768 MiB per-process commit limit, and a 1 GiB job-wide commit limit; assign the suspended process and retain desktop, job, process, and primary-thread handles until verified termination. Enforce a 30-second deadline from `ResumeThread`, an 8 KiB stdout cap, and a 64 KiB stderr cap. A timeout, cap breach, parse failure, unexpected child, or disposal kills the job. Cooperative child failures clean through their retained artifact leases; forced termination preserves and reports the exact residue path because killing the child destroys the only reparse-safe ownership proof. The launcher must never reopen and recursively delete that unproven path.
- Create and own the private desktop from one dedicated native-boundary worker thread so any desktop association made by `CreateDesktopW` cannot alter the launcher's main/test thread. Never call `SwitchDesktop`, `SetThreadDesktop`, `SetForegroundWindow`, `ShowWindow`, input APIs, or controller APIs. Preflight and postflight require the input-desktop identity to remain the same; a foreground-event observer requires that no foreground window during the run belongs to the child job, while allowing the user to change focus among their own applications. The child must attest the private desktop, which is closed only after the job is empty.
- Canonical argv order is `--quiet --path <exact-repository-game-directory> --screen 3 --position 684,-900 --resolution 1280x720 --windowed --audio-driver Dummy --rendering-method gl_compatibility -- --debug-base-projectile-evidence <exact-owned-absent-output-root>`. Quiet mode suppresses Godot logging; the launcher rejects additional engine, project, or user arguments.
- The projectile route writes exactly one newline-terminated completion marker directly to `Console.Out` and flushes it; it writes projectile error markers and all diagnostics directly to bounded `Console.Error`. It does not use `GD.Print`/`GD.PushError`, so `--quiet` cannot suppress its protocol. Preserve the existing incomplete-fidelity route's marker bytes and stream behavior. The literal stdout grammar and field order are `DEBUG_BASE_PROJECTILE_EVIDENCE_COMPLETE stateHash=6a25f798f6582a29 bulletId=0 ownerId=0 desktop=RoundsEvidence-<nonce> screen=3 windowX=684 windowY=-900 windowWidth=1280 windowHeight=720 viewportWidth=1920 viewportHeight=1080 assemblySha256=<assembly-sha> assemblyMvid=<assembly-mvid> pngSha256=<png-sha> frame=frame-0000.png`, where `<nonce>` is exactly 32 lowercase hexadecimal characters generated by the launcher, both SHA values are exactly 64 lowercase hexadecimal characters, and `<assembly-mvid>` is exactly 32 lowercase hexadecimal characters (`Guid` format `N`). No whitespace, sign, prefix, alternate case, field reorder, duplicate key, additional key, relative-name variant, or second non-empty stdout line is accepted.
- The launcher creates a dedicated anonymous acknowledgement pipe before `CreateProcessW` and passes only its inheritable read handle in the explicit environment as `ROUNDS_EVIDENCE_ACK_HANDLE=<unsigned-decimal-handle>`. After publishing and reverifying the terminal frame, the child emits the completion marker but retains its root/file identity leases and waits at most five seconds for exactly one `0x06` byte or pipe closure. While those leases prevent root/file replacement, the launcher opens identity-bound read handles, validates the exact root contents/hash/decode, and writes `0x06` only on success. The child then calls `CompleteVerifiedSingleFrame`, disposes the leases, and exits zero. Timeout, EOF, any other byte, or validation failure produces nonzero exit and child-owned cleanup; the launcher cannot report success until the acknowledgement, zero exit, and empty job all occur.
- The requested output root must be an absent, normalized absolute child of an existing non-reparse parent outside both the canonical candidate repository and the operating-system temporary directory; compare resolved parent handles/identities, not only path strings. The child retains the root/file identity leases through terminal publication. Success leaves only `frame-0000.png`; failure cleanup is limited to the child's still-retained proof as described above.
- Success requires zero exit, an empty job, unchanged input-desktop identity, no foreground event attributable to the job, exact marker fields, an owned `frame-0000.png` whose SHA-256 matches, complete RGBA8 decode at exactly 1920x1080, no partial or extra files, and no process or output-root handle leak. Preserve the finalized PNG on success; delete only the exact owned root on failure.
- Plan mode accepts injected topology, filesystem metadata, candidate/build identity, foreground/desktop identity, and output-root facts, emits the immutable launch plan or a refusal, and never creates a process, desktop, directory, or file. Unit tests exercise plan mode and native-boundary orchestration with fakes; they must not launch Godot or any GUI.
- Keep this ticket infrastructure-only: it enables later visual verification but does not itself prove the projectile matches installed ROUNDS. The ticket stays open until an attended execute-mode run at the exact reviewed candidate produces approved monitor-4 pixels.
- Do all implementation and verification headlessly with at most two logical processors; do not invoke execute mode, Godot, a renderer, a browser, a window, or any input mechanism while the user is playing.

## Evidence required

- Route tests prove the new argument is accepted only for debug builds, requires an absent absolute output root, remains mutually exclusive, and has no exposure through ordinary/release or replay routing.
- Factory tests prove repeated construction has one stable hash, active `arena-006`, vanilla profiles, and exactly one owner-0 base projectile produced by the simulation.
- Lifecycle tests prove both debug evidence routes are frozen and use distinct exact markers while the original marker bytes remain unchanged; projectile publication tests prove exact owner acquisition, create-new `frame-0000.png`, hash/dimension/decode verification, and precise failure cleanup.
- Plan tests cover every accepted field and refusal above, including device/ordinal/DPI/bounds drift, reparse/hash/version/build/commit mismatch, argv ordering/quoting, output containment, desktop identity, and marker grammar without native side effects.
- Native-boundary tests with fakes prove controls are ordered `create private desktop -> create suspended process -> configure job/priority/affinity/limits -> assign -> resume`, all handles remain owned, every failure closes the job before any cooperative output cleanup, forced termination preserves/reports unproven residue, no shell or activation/input operation is reachable, and success waits for an empty job.
- Source inspection proves the route reuses the existing one-frame capture lifecycle, uses the robust artifact owner, never reaches global input or continuous physics, and has no unowned native handle or unbounded output path.
- A zero-warning solution build, all applicable simulation and checker tests, repository checks, ticket format, and `git diff --check` pass under the bounded headless toolchain.
- An independent review approves the exact implementation commit. No native visual result or ticket closure is claimed until a later attended exact-candidate execute-mode run produces the owned PNG and complete projectile-specific attestation without disturbing the input desktop.

## Work log

- 2026-08-30T03:14:10Z stage design start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050a7-6cc6-7081-bf1d-7003d8eb12d5 — Split the missing transient-projectile evidence state from ticket 034's correction and ticket 032's broader closed-loop playtesting route.
- 2026-08-30T03:14:10Z stage design end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050a7-6cc6-7081-bf1d-7003d8eb12d5 — Bound one debug-only frozen state to real vanilla simulation APIs, one create-new absolute PNG, route-specific attestation, existing monitor checks, zero input, and zero continuous physics.
- 2026-08-30T03:14:10Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050a7-6cc6-7081-bf1d-7003d8eb12d5 — Extending the shared debug evidence protocol and deterministic match factory without launching Godot or any visible process.
- 2026-08-30T03:16:49Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050ab-1751-7bd3-82e0-739793113f2a — Cold-read the agent-proposed risk-4 contract for route isolation, real vanilla simulation state, create-new output, bounded lifecycle, and later visual evidence.
- 2026-08-30T03:18:01Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050ab-1751-7bd3-82e0-739793113f2a — Admitted at risk 4 with no findings. Uncommitted implementation had begun prematurely at 03:14:10Z and was paused before this independent verdict; no commit or integration occurred before admission.
- 2026-08-30T03:18:01Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050a7-6cc6-7081-bf1d-7003d8eb12d5 — Resumed the isolated uncommitted implementation after independent admission.
- 2026-08-30T03:23:54Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050a7-6cc6-7081-bf1d-7003d8eb12d5 — Added the debug-only frozen projectile route, real vanilla `arena-006` simulation state with stable hash `6a25f798f6582a29`, distinct attestation, and atomic create-new publication; passed a zero-warning bounded solution rebuild, 134 checker tests, 255 applicable simulation tests, repository checks, the exact 73-file identity boundary at `7bee1ee96a4a02ca32cba66ff04ca0403ba382d78bbd6779fb2d7e345d9bd185`, six-worktree/two-branch ticket format, and whitespace checks. No Godot, renderer, GUI, browser, GPU workload, or input ran, and no build or test process remained.
- 2026-08-30T03:24:56Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050b2-e2cc-7662-bd87-b3a3e7c3471e — Cold-reviewed exact range `be3918dc79ced5132e0caf95d84ec1573a398769..6ee2595089792b70a8e0bbd24042803890497544` against ticket 035.
- 2026-08-30T03:31:12Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/01a050b2-e2cc-7662-bd87-b3a3e7c3471e — Approved with no findings after independently reproducing the bounded build, 134 checker tests, 255 applicable simulation tests, exact 73-file identity hash, routing and cleanup invariants, repository checks, ticket format, and whitespace; native visual evidence remains intentionally pending.
- 2026-08-30T03:42:00Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Reopened the ready infrastructure ticket after an attempted launcher split exposed a circular dependency and seven missing safety/attribution boundaries. Preserved the already approved route history while folding launcher ownership back into the route's contract before any launcher implementation.
- 2026-08-30T03:43:00Z stage design end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Bound the later native run to a never-switched private desktop, exact DISPLAY4 physical topology, suspended direct process creation, pre-resume job/priority/affinity controls, retained native handles, pinned executable and assembly identity, reparse-safe absent-root publication, strict marker/PNG validation, and a side-effect-free plan/test seam. Native execution remains forbidden while the user is playing.
- 2026-08-30T03:44:00Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/readmit_035 — Cold-read the amended risk-4 route-plus-launcher contract for dependency order, private-desktop isolation, topology, process ownership, artifact ownership, exact attribution, and plan-only evidence.
- 2026-08-30T03:50:00Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/readmit_035 — Rejected pending five concrete repairs: a one-frame owner success transition, safe handling of ownership proof lost on forced termination, quiet-compatible direct protocol streams, exact runtime/build/resource bounds, and non-future work-log timestamps. The private-desktop, monitor mapping, suspended process, executable pin, handle lifecycle, plan-only seam, and dependency graph were otherwise coherent.
- 2026-08-30T03:51:00Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Added the verified single-frame owner transition; made forced-termination residue preservation explicit; moved the projectile protocol to direct bounded standard streams; fixed build executable, arguments, assembly path, timeout, output caps, and memory limits; and corrected the UTC work log before fresh admission.
- 2026-08-30T03:52:00Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/readmit2_035 — Fresh cold admission review of the corrected route-plus-launcher contract, including the prior findings and the remaining cross-process success boundary.
- 2026-08-30T03:55:00Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/readmit2_035 — Rejected pending three exact repairs: preserve the child's identity leases through parent PNG validation, force and pin the exact build/toolchain instead of accepting incremental ignored output, and define one literal completion-marker grammar. All prior corrections and the private-desktop/process containment remained coherent.
- 2026-08-30T03:56:00Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893 — Added a five-second inherited-pipe acknowledgement while the child retains artifact leases, forced a zero-warning `Rebuild` through the exact hash-pinned Visual Studio MSBuild and pinned SDK, fixed the actual 1920x1080 viewport/PNG boundary, and specified the complete literal marker grammar and encodings.
- 2026-08-30T03:57:00Z stage admission start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/readmit3_035 — Third fresh cold admission review of the current route-plus-launcher contract, with emphasis on the retained-lease acknowledgement boundary, exact build/runtime attribution, private-desktop process containment, inherited-handle allowlist, output validation, and fake-test seam.
- 2026-08-30T03:59:41Z stage admission end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/readmit3_035 — Admitted at risk 4 with no findings. The contract now closes every prior blocker with an exact four-handle `STARTUPINFOEX` allowlist, a child-retained reparse-safe lease through parent acknowledgement, a verified single-frame owner transition, forced-kill residue preservation, a hash-pinned forced rebuild and child assembly attestation, literal one-line completion grammar, and fail-closed private-desktop/job/topology controls; native execution and visual approval remain deferred.
- 2026-08-30T04:00:00Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Began the admitted route/ownership/protocol/planning slice in the isolated hardening worktree, with native execution forbidden while the user was playing.
- 2026-08-30T04:13:05Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Replaced the bare projectile PNG with absent-root retained ownership and verified single-frame completion; added the Godot RGBA8 decoder, actual Win32 thread-desktop query, runtime assembly hash/MVID, exact direct-stream marker parser, inherited-handle acknowledgement with exact child-side disposal, and a pure fail-closed topology/toolchain/output launch plan plus Windows argument encoder. A zero-warning bounded solution build, 21 focused cases, 266 applicable simulation cases, and all 134 checker cases passed; the reviewed 75-file runtime boundary is `6062124728f94dce1003d7f2adbea5de1484100d5a4f68f1becce165f3eacf58`. The detached-worktree-only `GoldenHistoryScriptTests` group still cannot find a worktree-local `.tools/dotnet` executable and was excluded after its unrelated probe; no test process remained. No Godot, renderer, GUI, browser, or input ran. The native `STARTUPINFOEX`/private-desktop/job/topology execute boundary, fake-native ordering tests, immediately preceding pinned rebuild driver, parent-side identity-bound PNG validation/acknowledgement, and attended pixels remain explicitly deferred; this ticket stays open.
- 2026-08-30T04:14:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Cold-reviewed exact range `7e3852777c105f648dfe0602d9fcaa8018ddf7df..f527e1a49c2aca478d687cc001d74a5d39094d1f` against the admitted route/planner slice without native execution.
- 2026-08-30T04:18:59Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Rejected six exact findings: Windows CRLF could not pass the LF-only parser, output containment lacked identity-resolved ancestry, the process command line omitted executable `argv[0]`, acknowledgement accepted trailing bytes and used wall time, single-frame completion trusted caller-copied terminal state, and ordinary decoder limits were widened.
- 2026-08-30T04:19:00Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Correcting six independent review findings in a new commit: literal-LF emission, full identity-resolved ancestor proof, executable `argv[0]`, monotonic exact-one-byte acknowledgement framing, stored response identity, and a separate exact-size evidence decoder.
- 2026-08-30T04:34:03Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Completion now writes the marker plus literal LF; output planning requires a continuous, bound, non-reparse canonical proof for every ancestor and refuses repository/temp aliases; the modeled CreateProcess command line round-trips executable `argv[0]` plus all arguments; acknowledgement uses `Stopwatch`, requires one `0x06` followed by pipe closure, and rejects trailing bytes; single-frame completion requires the exact stored terminal response instance; and ordinary 1280x720 decode bounds remain separate from exact 1920x1080 evidence decode. The zero-warning bounded solution build, 21 focused cases, 274 applicable simulation cases, all 134 checker cases, ticket format, and whitespace passed; the reviewed 76-file runtime boundary is `c374578be970695baa09aadf01fdfbaeecbd75e9f6a0635f776c9661b6a5f4d6`. No Godot, renderer, GUI, browser, input, or visible process ran. The native execute boundary and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T04:35:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-reviewed corrected exact range `7e3852777c105f648dfe0602d9fcaa8018ddf7df..ab61a66bacbe123907b101ccba9684a34b396796`, prioritizing closure of all six findings and correction regressions.
- 2026-08-30T04:38:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. All six issues are closed, the correction introduced no new defect, 29 focused exact-candidate tests passed independently, and tracked state plus whitespace were clean. Approval covers the headless route/planner hardening; the native execute boundary and visual evidence remain deferred.
- 2026-08-30T04:52:16Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added an inert standalone evidence-launcher executable and injected native-boundary coordinator. Fake-native tests prove the pinned immediate rebuild attestation, dedicated-worker preflight, exact private-desktop/suspended-process/job/assign/resume order, exact four-handle allowlist and decimal ACK environment handoff, shared resume deadline and pipe caps, identity-bound parent PNG validation before ACK, zero-exit/empty-job/postflight success, job-first cleanup, and exact forced-residue reporting without any launcher cleanup capability. The zero-warning bounded eight-project solution build, 12 focused launcher cases, 286 applicable simulation cases, all 134 checker cases, repository checks, and whitespace passed; the reviewed shipped runtime/build boundary is `795aa2aaf2938491f2c6f4b69936076a1592a6352ade2a4f13df706eb19793cb`. No native call, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete Win32 fact collection and `STARTUPINFOEXW`/private-desktop/job/pipe/foreground implementations, explicit execute wiring, native handle-leak/termination evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T05:07:31Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed all five launcher-boundary review findings after first reproducing six failures in the fake seam. The process lease now terminates an unassigned suspended process on disposal and transfers termination ownership only after successful kill-on-close job assignment; every failure after acknowledgement conservatively reports the exact preserved output root; build and immediately-pre-create preflight attest clean identity-bound candidate, opened MSBuild/Godot identities and pinned versions/hashes, and the immediate runtime assembly path/hash/MVID; an owned foreground observer spans pre-resume through empty-job and detects transient job windows; and ACK handle zero is rejected. A zero-warning bounded eight-project solution build, 32 focused launcher cases, 306 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the reviewed shipped runtime/build boundary is `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native execute, Godot, renderer, GUI, browser, input, or visible process ran. The concrete Win32 implementation and attended visual evidence remain deferred, so the ticket stays open.
- 2026-08-30T05:17:20Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Bound process creation to retained opened-executable leases: the build driver receives the exact opened MSBuild lease and attests the build process image, while the native coordinator passes the retained Godot lease into suspended creation and independently matches the child image before job setup or resume. Replacement and lease mismatches fail closed. Cleanup now aggregates disposal failures through nested `try/finally` so every owned job, observer, process/thread, pipe, executable, and desktop lease is attempted; an unassigned process is still terminated when job disposal throws. The zero-warning bounded eight-project build, 42 focused launcher cases, 316 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native execute, Godot, renderer, GUI, browser, input, or visible process ran. Concrete Win32 execution and attended visual evidence remain deferred, so the ticket stays open.
- 2026-08-30T05:22:22Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Kept the explicit process terminate-and-wait fallback armed after job assignment and resume; it is now disarmed only after zero process exit and empty-job proof. A throwing job close before that proof cannot suppress the process fallback or later cleanup, while verified-complete success performs no redundant termination. The zero-warning bounded eight-project build, 43 focused launcher cases, 317 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native execute, Godot, renderer, GUI, browser, input, or visible process ran. Concrete Win32 execution and attended visual evidence remain deferred, so the ticket stays open.
- 2026-08-30T05:23:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Final cold review of exact launcher-boundary range `89ccbf5c09d75f7e5e12489047546ec00ef113e5..498a1df84a61dda175243eb3098c0374822550c1` after iterative findings on pre-job orphaning, post-ACK residue, immediate executable attribution, foreground coverage, handle framing, exception-independent cleanup, and job-close fallback.
- 2026-08-30T05:25:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. The inert standalone launcher boundary now retains exact executable leases through process-image attestation, proves full-run foreground observation and artifact acknowledgement ordering, and cannot orphan an unassigned or assigned process even when job disposal throws. Tracked state and whitespace were clean; concrete Win32 execution and attended pixels remain deferred.
- 2026-08-30T05:35:37Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the first compile-only concrete Win32 foundation behind the still-inert execute branch: pinned x64 ABI layouts/constants and dormant declarations for private desktops, retained files, `STARTUPINFOEXW`, jobs, anonymous pipes, process termination, and waits; exact-once executable, desktop, process/thread, job, and seven-handle pipe/acknowledgement leases now implement the approved ownership seams. Six fake-shim regressions exercise ABI size/offsets, the exact four-handle child allowlist, one-byte acknowledgement framing, termination/wait fallback, verified disarm, and cleanup without invoking any native API. The zero-warning bounded eight-project build, 49 focused launcher cases, 323 applicable simulation cases, all 134 checker cases, repository checks, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No execute command, native implementation, Godot, renderer, GUI, browser, input, or visible process ran. Production factories and wiring for DPI/topology, dedicated-thread desktop/input identity, file/hash/version identity, environment/attribute-list/process creation, job configuration/assignment/empty proof, bounded asynchronous pipe drains, foreground observation, child-image identity, identity-bound PNG validation, the build driver, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T05:47:43Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed four concrete-foundation review findings. A successful suspended-process return now immediately transitions the pipe lease by closing exactly the parent copies of child stdin, stdout, stderr, and acknowledgement handles, leaving only the parent read/read/write endpoints and making later EOF observable; the transition is exact-once, idempotent, and refuses after disposal. The acknowledgement writer closes through `finally` even when the injected write throws. Process fallback uses an exact five-second wait, reports terminate failure or timeout, and still releases both process handles. The launcher now compiles x64-only and rejects a non-eight-byte pointer size before parsing either mode, while x64 execute remains inert. The zero-warning bounded eight-project build, 57 focused launcher cases, 331 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native API, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; all previously recorded concrete production factories, execute wiring, native evidence, and attended pixels remain deferred.
- 2026-08-30T05:48:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Cold-reviewed exact dormant Win32 foundation range `c649b0d496f51eaf4a3bb66ab0a190632122c06e..076d43f1b4d6fb9b602a30d481fb161dddff76c3`, including ABI signatures, lease ownership, inherited handle transitions, acknowledgement cleanup, bounded termination, architecture enforcement, and execute reachability.
- 2026-08-30T05:52:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings after the correction. Parent copies of child pipe ends close immediately after process creation, acknowledgement cleanup is exception-safe, termination cannot wait indefinitely, x64 is enforced before parsing, the full dormant slice introduced no ownership or ABI regression, and execute remains inert.
- 2026-08-30T05:58:14Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added two dormant concrete factories behind the inert launcher entry. The topology factory establishes and verifies `PER_MONITOR_AWARE_V2` before calling the production `EnumDisplayMonitors`/`GetMonitorInfoW` adapter, preserves callback ordinal and physical pixel bounds, and refuses missing or duplicate exact `\\.\DISPLAY4` identities. The desktop factory opens, names, and closes an input-desktop identity handle; creates only a nonce-bound `RoundsEvidence-<32-lower-hex>` desktop from a dedicated worker context with create/read/write access and no switch access; reverifies its actual object name; and closes every failed or successful lease exactly once. Twelve new tests use injected topology, desktop, and worker shims or the isolated managed worker only. The zero-warning bounded eight-project build, 69 focused launcher cases, 343 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Retained no-delete file/executable open/final-path/file-ID/hash/version factories remain deferred along with environment/attribute-list/process creation, job operations, bounded pipe drains, foreground observation, child-image identity, parent PNG validation, build driver, coordinator wiring, native evidence, and attended pixels; the ticket stays open.
- 2026-08-30T06:08:09Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Made DPI collection repeatable without weakening the pre-enumeration proof. Every collection still attempts `SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)` first, then treats the immediately queried effective context as authoritative: exact PMv2 proceeds even when Set reports false because the process was already configured, while any other or unknown context refuses before enumeration. Regressions prove first-set/second-already-set collections return identical topology, set-false/exact-PMv2 succeeds, and both set results refuse when the effective context is not PMv2. The zero-warning bounded eight-project build, 71 focused launcher cases, 345 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; prior deferrals remain unchanged.
- 2026-08-30T06:09:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-reviewed exact topology/private-desktop factory range `b40d778448aa5d2dd2e349698d7c3aa8c267133d..511befcbf7b1e69f6059731f95524774c43a18c0`, emphasizing repeatable PMv2 proof and worker-owned desktop cleanup.
- 2026-08-30T06:10:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. Exact effective PMv2 remains authoritative across repeated collections, every other context refuses before enumeration, topology is stable, the desktop factory has no prohibited switch/input reachability, and Program remains inert.
- 2026-08-30T06:19:51Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant retained executable-identity factory and production Win32 adapter. It normalizes an absolute DOS/UNC request, rejects ADS/device/extended-input/directory shapes, opens with `GENERIC_READ`, `FILE_SHARE_READ` only, and `FILE_FLAG_OPEN_REPARSE_POINT`, then retains that no-write/no-delete handle through two exact file-ID/final-path/metadata snapshots, bounded SHA-256 streaming with original-position restoration, and exact file/product-version reads. It refuses directories, reparse points/tags, hardlinks, delete-pending/empty/oversized files, zero identities, canonical-path aliases, hash/version mismatch, and any path/file-ID/size/change-time/attribute drift; success exposes the same retained handle for later build/process creation and all failures or lease disposal close it exactly once. Separate pinned Godot/MSBuild profiles cap executable size at 512 MiB. Twenty-eight new cases use injected file/stream/version shims only and pin the x64 metadata ABI. The zero-warning bounded eight-project build, 99 focused launcher cases, 373 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Environment/attribute-list/process creation, job operations, bounded pipe drains, foreground observation, child-process image identity, parent PNG validation, concrete build/coordinator wiring, native evidence, and attended pixels remain deferred; the ticket stays open.
- 2026-08-30T06:20:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Cold-reviewed exact retained executable identity range `585de99e6b1ba6df62d47d65f4196ac944a567f5..abdd1a15244f23d0576c5cdb9f4491df4451d999`, including path shape, handle share/access, final-path/file-ID binding, hashing, version, drift, and close semantics.
- 2026-08-30T06:27:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. The retained handle blocks replacement while exact path, identity, hash, versions, and two snapshots are verified; every refusal and successful lease closes exactly once; the same handle remains available for later process creation; Program remains inert.
- 2026-08-30T06:38:43Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant concrete suspended-process factory behind the inert launcher entry. It builds a canonical explicit Unicode environment from a four-variable OS allowlist plus exact contract variables; rejects duplicate, malformed, NUL-bearing, or altered desktop/acknowledgement entries; allocates, initializes, updates, deletes, and frees a `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` containing exactly the four admitted child handles; and supplies an exact `STARTUPINFOEXW` desktop/std-handle contract to direct `CreateProcessW` with the retained Godot path, executable-inclusive mutable command line, exact flags, and game working directory. Successful creation immediately closes parent copies of child pipe ends, retains armed process/thread and executable leases, and independently compares the child image's final path, file ID, hash, and versions before any job or resume boundary; all allocation, attribute, partial-process, image, and cleanup failures close or terminate every acquired resource. Twenty-five new tests exercise only injected managed shims. The zero-warning bounded eight-project build, 124 focused launcher cases, 398 applicable simulation cases, and all 134 checker cases passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Job operations, bounded pipe drains, foreground observation, parent PNG validation, concrete build/coordinator wiring, native evidence, and attended pixels remain deferred; the ticket stays open.
- 2026-08-30T06:52:19Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Corrected both dormant native-lifetime findings. A dedicated attribute-list lease now owns both the initialized `PROC_THREAD_ATTRIBUTE_LIST` allocation and the exact four-handle `lpValue` backing allocation, keeps both live through `CreateProcessW`, then deletes the attribute list before freeing the value and list allocations; initialization, value allocation/write/update, create, delete, and free failures retain exact cleanup ordering without double-free. Process command-line and environment pins now use injected exact-once leases acquired inside an exception boundary established before the first pin: both stay live through the direct process invocation, release in reverse order, and a second-pin failure releases the first without invoking process creation. Five additional fake-only regression cases brought the focused process-creation matrix to 30 cases. The zero-warning bounded eight-project build, 403 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; prior deferrals remain unchanged and the ticket stays open.
- 2026-08-30T06:53:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-reviewed exact dormant suspended-process factory range `9148d3ea8ab12366d14a1e9ae08d6f36fedf475a..ff9dcd99e4123fbadab1313ba47885ca63b0d946`, emphasizing native backing lifetimes and partial-allocation cleanup.
- 2026-08-30T06:54:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. The handle-list value remains live through process creation, attribute cleanup order is exact, pin acquisition and reverse cleanup cover second-allocation failure, no factory regression remains, and Program is still inert.
- 2026-08-30T07:02:23Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant concrete Job Object controller and production Win32 adapter behind the inert launcher entry. It creates only an unnamed retained job; validates and applies an exact 144-byte `JOBOBJECT_EXTENDED_LIMIT_INFORMATION` with kill-on-close, one active-process limit, affinity, below-normal priority, process memory, and job memory flags and admitted values; refuses weakened limits without creating a handle; checks whether the still-suspended child already belongs to a parent job; assigns it before marking either job or process ownership; and accepts only `ResumeThread` previous suspend count one after starting the monotonic deadline. Exact 48-byte basic-accounting queries poll with bounded monotonic 10 ms/remaining delays until active processes reach zero, while query, size, counter, and timeout drift fail closed. Terminate-job or close failure leaves the explicit process terminate/wait fallback armed. The production declarations include no breakaway or UI-restriction path because the admitted contract has neither. Twenty-four fake-only cases cover exact ABI/limits/order/state, create/configure/assignment/nested-job/resume failures, bounded empty proof, termination fallback, and exact-once handles without invoking P/Invoke. The fresh worktree performed a locked, nonparallel restore from the existing cache; the zero-warning bounded eight-project build, 24 focused job cases, 427 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Bounded asynchronous pipe drains, foreground observation, parent PNG validation, concrete build/coordinator wiring, execute wiring, native evidence, and attended pixels remain deferred; the ticket stays open.
- 2026-08-30T07:07:20Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Narrowed the dormant Job Object boundary from merely bounded values to the ticket's exact admitted literals. Configuration now refuses before any native create or set call unless affinity is `0x3`, active-process limit is `1`, process commit is exactly `768 * 1024 * 1024`, job commit is exactly `1024 * 1024 * 1024`, priority is below-normal, and kill-on-close is true. Resume likewise refuses before reading the clock or touching the thread unless the deadline is exactly 30 seconds. Regressions cover affinity `0x1`/`0x7`, active-process `2`, smaller and larger positive values for both memory limits, false priority/kill flags, 29/31-second deadlines, and exact success. The zero-warning bounded eight-project build, 32 focused job cases, 435 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; prior deferrals remain unchanged and the ticket stays open.
- 2026-08-30T07:08:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-reviewed exact dormant Job Object range `d571ddbbe5576bcba4d0ae71d2b66bfddc865368..a98da0eae588d649c2586353644c0faee6bdaf16`, including literal limits, assignment, resume, empty proof, and fallback ownership.
- 2026-08-30T07:09:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. Every deviation from the exact admitted limits or 30-second deadline refuses before a native-facing operation; ABI, suspended assignment, ownership transfer, bounded active-zero proof, and termination fallback remain intact; Program is inert.
- 2026-08-30T07:12:58Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the next dormant concrete boundary: concurrent bounded stdout/stderr drains sharing the already-started monotonic run deadline, exact raw-byte caps and EOF requirements, strict UTF-8 and single-LF marker framing, read-fault/cancellation handling, and exact parent-read-handle ownership. This slice remains unwired from execute mode; all verification will use injected fakes without native, Godot, renderer, GUI, browser, or input activity.
- 2026-08-30T07:20:19Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant production bounded pipe-capture slice behind the inert launcher. Successful process creation can now transfer the retained parent stdout/stderr handles exactly once to a capture lease; two concurrent nonblocking Peek/Read drains share the already-started exact 30-second monotonic deadline, propagate read/cancellation faults, close both handles through exception-safe cleanup, and do not touch the acknowledgement writer. The exact 8,192-byte stdout and 65,536-byte stderr caps count every raw byte including stdout's terminal LF: exactly cap bytes proceed, while the first cap+1 sentinel sets the corresponding failure and retains only the exact capped prefix. Success requires EOF on both streams, rejects UTF-8 BOM and decoder replacement, preserves raw bytes, accepts stdout only as one exact LF-terminated base-projectile completion marker with no CR/extra bytes/lines, and treats stderr only as bounded diagnostics. Twenty-five injected fake-only cases cover concurrent output beyond a typical pipe buffer, one-sided early EOF, partial/zero-byte reads, exact cap/cap+1, strict UTF-8, framing, shared deadline, cancellation, read faults, wrong parameters, raw preservation, ACK separation, and exact-once ownership. The fresh worktree performed a locked nonparallel restore from the existing cache; the zero-warning bounded eight-project build, 25 focused pipe cases, 460 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation was called and no execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Real Windows pipe behavior remains unexercised until the full native boundary is reviewed; foreground observation, parent PNG validation, concrete build/coordinator wiring, execute wiring, native evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T07:25:03Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Review of exact candidate `fbdd3d4e688c8ef148d6c8c263331276a8873746` found two pipe-boundary defects: successful zero-byte activity and `ERROR_NO_DATA` were incorrectly accepted as EOF instead of awaiting the sole anonymous-pipe EOF proof, `ERROR_BROKEN_PIPE`; and a synchronous second-drain startup failure could dispose cancellation and read-handle ownership before the already-started first drain was observed. Correction was required before approval.
- 2026-08-30T07:28:54Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the narrow correction. The native adapter will expose injected last-error behavior without invoking P/Invoke, zero-byte/`ERROR_NO_DATA` activity will continue against the shared deadline until explicit broken-pipe EOF, and injected drain startup will cancel and await every started task before closing retained read handles. Program remains inert and all verification remains fake-only and headless.
- 2026-08-30T07:31:33Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Corrected both reviewed pipe-boundary defects without wiring the dormant launcher. Successful zero-byte reads, zero-byte injected activity, and `ERROR_NO_DATA` now mean no progress and continue against the shared monotonic deadline; only failed native Peek/Read with `ERROR_BROKEN_PIPE` maps to anonymous-pipe EOF. The production adapter's last-error seam is injectable, and regressions prove one or repeated zero-byte activities need a later explicit EOF, no later EOF times out, `ERROR_NO_DATA` never completes either adapter path, and broken-pipe does. Both drain starts now pass through an injected starter; a synchronous second-start failure cancels and awaits/observes the first task while the cancellation source and both read handles remain live, then cleanup closes each handle exactly once without ACK. A transient focused assertion failure was narrowed to the valid shared-stop race and rerun after rebuilding. The zero-warning bounded eight-project build, 33 focused pipe cases, 468 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; all prior native-evidence and attended-pixel deferrals remain unchanged, and the ticket stays open.
- 2026-08-30T07:32:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-reviewed the corrected cumulative pipe boundary `f2ae4454bd370830a22b3828e5ef69d844984b0c..41ba121d1bdf2f69128c31d60edd79b0be5a469f`, including both prior findings, native last-error translation, started-task lifetime, exact byte/deadline bounds, and the narrowed shared-stop assertion.
- 2026-08-30T07:33:00Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. Only `ERROR_BROKEN_PIPE` proves anonymous-pipe EOF; zero-byte activity and `ERROR_NO_DATA` continue against the shared deadline; every started drain is cancelled and observed before cancellation-source or read-handle cleanup on startup failure. Exact caps, strict UTF-8 and marker framing, raw bounds, acknowledgement separation, and the inert Program remain intact. The shared-stop assertion still requires timeout and explicitly rejects false EOF.
- 2026-08-30T07:38:19Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the dormant foreground-activation observer boundary. The slice will install the exact foreground WinEvent range on a dedicated message-loop worker, root and serialize the managed callback through same-thread unhook, classify transient foreground HWND processes against a synchronized borrowed job handle, retain every asynchronous failure, and stop/join boundedly without manipulating windows or polling. Program and execute remain inert; tests will use injected managed/native shims only and will not invoke P/Invoke, Godot, renderer, GUI, browser, input, or any visible process.
- 2026-08-30T07:51:41Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant foreground-activation observer and concrete Win32 declarations behind the inert launcher. A dedicated background worker establishes its message queue before publishing readiness, installs exactly `EVENT_SYSTEM_FOREGROUND..EVENT_SYSTEM_FOREGROUND` with `WINEVENT_OUTOFCONTEXT`, roots the managed callback with a retained `GCHandle`, and runs a complete `GetMessageW`/`TranslateMessage`/`DispatchMessageW` loop before exact installing-thread unhook. Valid callbacks require the documented hook/HWND/`OBJID_WINDOW`/`CHILDID_SELF` shape; reentrant delivery is serialized through an explicit callback queue; HWND-to-PID, minimal-query process open, exact-job membership, transient close, malformed/disappearance, hook, loop, callback, and lifecycle failures are retained and make stop fail closed without crossing the unmanaged callback boundary. Stop posts quit and joins with exact five-second bounds, drains callbacks queued ahead of quit, and is idempotent with dispose. Failed post or unproven join never releases worker-owned signals, callback state, or thread state: the late worker owns same-thread unhook and exact-once deferred cleanup after proven completion. `Win32JobLease` now provides a synchronized, nonduplicating borrowed-handle operation so job close cannot race a callback into a reused handle and the observer never delays kill-on-close by owning a duplicate. Thirty-four fake-only cases cover ABI/constants/inert entry, exact readiness/range/flags/full loop/thread identity, unrelated/repeated/reentrant/sticky classification, fail-closed malformed/errors/disappearance, exact transient closes, startup and stop failures, queued event ordering, idle forced job-first cleanup, callback-vs-job-close races, and late unjoined-worker cleanup with no unhandled background exception. An initial full-suite scheduler-dependent race test was corrected to use a dedicated disposer and explicit `finally` release, then the same full gate passed. The locked nonparallel restore, zero-warning bounded eight-project build, 34 focused observer cases, 502 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Real Win32 hook/message behavior remains unexercised until the full native boundary is reviewed; parent PNG validation, concrete build/coordinator/execute wiring, native evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T07:59:08Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Review of exact candidate `fb5bc6bde1e7f279ea9f8ff45cc33fb9b1342023` found three lifecycle gaps: a failed quit post permanently marked stop complete instead of allowing a later dispose/stop retry; failed unhook cleared the hook and freed the `GCHandle` before external join proved installing-thread exit; and the job-dispose race lacked an explicit disposer-entered synchronization point. Correction was required before approval.
- 2026-08-30T07:59:08Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the narrow lifecycle correction. Unproven termination will remain retryable with every prior failure retained, failed unhook will keep hook identity and callback root until a successful external join proves automatic hook removal, and deterministic tests will distinguish disposer entry from scheduler delay. Program remains inert and all verification remains fake-only and headless.
- 2026-08-30T08:05:07Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Corrected all three reviewed observer lifecycle gaps. A failed quit post or unproven join no longer terminalizes stop: each explicit stop/dispose retries one bounded post and join while retaining every prior failure and all worker-owned state until thread termination is externally proven. Successful later cleanup remains fail-closed because the original failure is preserved; repeated post failure retains ownership safely for a later retry. Failed or throwing same-thread unhook now keeps the hook identity and managed callback `GCHandle` rooted until a successful external join proves installing-thread exit and Windows' automatic hook removal, after which one reaper releases resources exactly once. The job-close race now signals actual disposer entry immediately before `Job.Dispose`, distinguishes lock blocking from scheduler delay, and always releases membership in `finally`. The zero-warning bounded eight-project build, 34 focused observer cases, 502 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Real Win32 hook/message behavior remains unexercised until the full native boundary is reviewed; parent PNG validation, concrete build/coordinator/execute wiring, native evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T08:14:23Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Review of exact correction head `fe02be59d0bc5b0950a34502066d06a439089991` found one remaining startup ownership gap: readiness timeout could leave a started, unjoined worker without a returned lease or any retained retry owner. The prior explicit-stop retry, failed-unhook root lifetime, and job-disposer synchronization corrections remained sound.
- 2026-08-30T08:14:23Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the startup-only ownership correction. Abandoned startup will transfer to an injected strong cleanup owner only when the initial bounded stop cannot prove termination; late workers will check abandonment before hook install and before the blocking loop, while a background reaper retries bounded stop/join attempts with blocking low-CPU backoff and retains state fail-closed on scheduler failure. Program remains inert and verification remains fake-only and headless.
- 2026-08-30T08:16:26Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed the startup ownership gap without changing the observer's normal message or callback ordering. A readiness failure now marks startup abandoned before its initial bounded stop; only an unproven started worker transfers to an injected cleanup owner whose production instance holds a static strong reference, schedules a dedicated background reaper, retries the same bounded post/join path with an exact one-second blocking backoff, records every failure without throwing from the reaper, and releases ownership only after external join plus callback-root, thread, and signal cleanup are all proven. Scheduler failure leaves the lease strongly retained for process-exit fallback. The late worker checks abandonment after queue establishment before hook installation and again immediately before the blocking loop, so both gated pre-install and install-race cases self-exit; the latter still performs same-thread unhook. Deterministic tests prove a failed initial stop needs a later reaper retry, no hook is installed in the pre-install case, the install race never enters `GetMessage`, successful reaping releases exactly once, scheduler failure remains explicitly owned, and no test worker leaks or reports an unhandled exception. The zero-warning bounded eight-project build, 36 focused observer cases, 504 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed; the shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Real Win32 hook/message behavior remains unexercised until the full native boundary is reviewed; parent PNG validation, concrete build/coordinator/execute wiring, native evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T08:18:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Cold re-reviewed the exact cumulative foreground-observer range `fcfdacb93412ef3cd27b4f95c4bd7bead90dda37..ad885778b045e65ffb67d825fde1019ec1110e1b`, prioritizing abandoned-startup ownership, both late-worker races, bounded reaper behavior, and every prior lifecycle correction.
- 2026-08-30T08:19:35Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. Every started, unjoined startup failure transfers to a strong cleanup owner; pre-install and post-install abandonment both self-exit; the low-CPU reaper contains failures and removes ownership only after external join and exact cleanup; scheduler failure remains strongly retained. Prior stop retry, failed-unhook callback-root lifetime, worker-exit proof, job-borrow synchronization, ABI, message-loop, fail-closed classification, test cleanup, and the inert Program remain correct. Review was static because the user was gaming; no native API, Godot, GUI, renderer, browser, or input activity occurred.
- 2026-08-30T08:24:31Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the dormant parent-side published-frame validator. The slice will retain and reverify exact root/frame handle identities and contents before acknowledgement, read bounded bytes only through the frame handle, require the literal frame plus internal owner marker and no other entry, hash exact bytes, and fully parse CRC/framing/zlib/filter-complete RGBA8 PNG data in managed code. Program and coordinator wiring remain inert; all tests will use injected fakes without native, Godot, renderer, GUI, browser, input, or visible activity.
- 2026-08-30T08:39:29Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant production parent-side published-frame boundary and closed its pre-ACK lifetime gap. Exact root and `frame-0000.png` handles open without write sharing but with delete sharing compatible with the child's DELETE leases; the exact owner marker must be present and an injected/native probe must fail specifically because its child `FileShare.None` lease is live. Root contents enumerate twice through the retained directory handle using pinned `FileIdBothDirectoryRestartInfo`/continuation parsing, allowing exactly the frame and marker; frame streams enumerate by handle and reject every ADS. Same-volume nonzero identities, exact final paths/case, non-reparse/non-delete-pending kinds, one frame link, length/change/attributes/tags, and enumeration remain stable around exact retained-handle read, lowercase SHA-256, and decode. The managed bounded PNG decoder verifies signature, chunk letters/order/length/CRC, exact noninterlaced 1920x1080 RGBA8 IHDR, consecutive IDAT and terminal IEND, strict zlib header/complete deflate consumption/Adler-32/exact decompressed size, and filters 0–4 including Paeth; it rejects compressed trailing bytes despite inflater buffering. Validation now returns a disposable root+frame lease retained by the orchestrator across acknowledgement, zero child exit, and empty-job proof; cleanup closes the job first, then validation frame/root handles in reverse order, aggregating every failure without skipping later leases. Seventy-one validator/parser/decoder fake-only cases and 49 orchestrator lifetime cases passed; the zero-warning eight-project build, 576 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed after a locked cached restore. The shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation was called and no execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete native-boundary composition/build wiring, execute enablement, live Windows evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T08:55:02Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the exact three-finding artifact-validator correction: supported PNG color-chunk order/value legality, an independent successful-page ceiling for retained-handle directory enumeration, and nonoverlapping checked continuation framing in both native page parsers. Program remains inert and verification remains fake-only and headless.
- 2026-08-30T08:57:59Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed all three reviewed artifact-validator findings without changing retained-handle lifetime or wiring execute. The strict RGBA8 PNG decoder now requires cHRM/gAMA/sBIT/sRGB before both PLTE and IDAT, deliberately narrows cHRM to canonical sRGB chromaticities, bounds every bKGD sample to eight bits, and rejects bKGD before a later PLTE while accepting exact PLTE-then-bKGD placement. Retained-handle root enumeration now admits at most four successful native pages independently of emitted entries or name bytes, so repeated dot-only pages fail closed without another call. Both directory and stream page parsers use checked-width fields and require each nonterminal continuation to begin at or after the complete current header and name, rejecting overlap and 32-bit framing overflow. Fourteen targeted fake-only regressions brought the focused validator/parser/decoder suite to 85 cases. The zero-warning bounded eight-project build, 590 applicable simulation cases, all 134 checker cases, repository checks, ticket format, and whitespace passed using cached inputs and single-node tooling. The shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; concrete native composition/build wiring, execute enablement, live Windows evidence, and attended pixels remain deferred, so the ticket stays open.
- 2026-08-30T09:01:47Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the remaining narrow PNG transfer-characteristic correction: retain gAMA presence/value and require canonical integer 45455 whenever sRGB coexists, in either chunk order. No native boundary or launcher wiring changes are in scope.
- 2026-08-30T09:02:57Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed the final PNG transfer-characteristic finding. The decoder now retains the parsed gAMA value and sRGB presence, accepting standalone nonzero gAMA as before but requiring exact big-endian integer 45455 whenever sRGB coexists, regardless of whether gAMA or sRGB appears first. Four order-specific regressions prove canonical coexistence succeeds and noncanonical coexistence refuses in both orders. The zero-warning bounded eight-project build, 89 focused validator/parser/decoder cases, 594 applicable simulation cases, all 134 checker cases, and repository checks passed. The shipped runtime/build boundary remains `26b8539413e6ce37df506e7a24b8a6fe63e65c803ed46c2278a44826ba7ce4bf`. No P/Invoke/native implementation, execute mode, Godot, renderer, GUI, browser, input, or visible process ran; prior deferrals remain unchanged and the ticket stays open.
- 2026-08-30T09:03:20Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Cold-reviewed the exact cumulative retained-frame validator range `db609f5c8b937dbf96bc06482bbfca225b3b26ef..850a06ce88ad875ec8df72048d1fee6e9aca8462`, including the retained validation lease, handle-bound root/frame/stream proof, child-marker liveness, strict managed PNG/zlib boundary, cleanup ordering, and every iterative correction.
- 2026-08-30T09:06:12Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. Canonical gAMA/sRGB coexistence is enforced in either order; successful dot-only directory pages are independently bounded; native page records cannot overlap their names; root/frame identities, contents, streams, bytes, hash, and decoded pixels remain proven through acknowledgement, zero child exit, and empty-job proof; and cleanup closes the job before the retained frame/root handles. Program remains inert. Review was static while the user was gaming, so no native API, Godot, renderer, GUI, browser, input, or visible process ran. Concrete composition/build wiring, execute enablement, live Windows evidence, and attended monitor-4 pixels remain deferred, so the ticket stays open.
- 2026-08-30T09:10:48Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the dormant concrete pinned-MSBuild rebuild and runtime-assembly attestation driver. The slice will validate the exact invocation and cached locked prerequisites, retain the opened MSBuild identity through an injected direct-process boundary, require exact child-image attribution and bounded zero-warning completion, prove prior runtime deletion and immediate recreation, and hash/read MVID from a stable retained runtime handle. Program and execute composition remain inert; all new tests use fakes and no process/native/GUI activity.
- 2026-08-30T09:26:43Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added the dormant pinned rebuild coordinator contract and concrete retained runtime-closure factory without composing execute. The exact MSBuild argv now includes `/noAutoResponse`; the driver refuses any argument drift, uses a thirteen-key non-inherited effective-environment allowlist, pins `global.json`/SDK 8.0.423/ref pack 8.0.29 and exact identity-bound Game/Replay/Sim project-lock-assets plus four Godot 4.7.1 package-cache inputs, and rechecks the clean repository identity before deletion, immediately before spawn, and after build. Its direct-process seam requires suspended creation, exact retained MSBuild image match before resume, a kill-on-close affinity-`0x3` one-process admitted-memory job, concurrent capped pipes, five-minute deadline, independently attested effective environment, EOF, zero exit/warnings, and empty job. It proves the prior Game output absent before spawn and a distinct exact output afterward, then the production retained-file factory hashes and parses exact managed PE assembly names/MVIDs for Game/Replay/Sim through three stable no-write/no-delete handles while a required ancestor lease proves reparse-free chains. The returned disposable build-attestation lease now stays owned across native preflight, Godot use, child attestation, zero exit, and empty-job cleanup; orchestrator tests prove job cleanup precedes its reverse closure and every MSBuild/runtime lease close failure is fail closed. A locked single-node cached restore generated ignored assets only. The zero-warning bounded eight-project build, 91 focused build/orchestrator cases, 636 applicable simulation cases, all 134 checker cases, and repository checks passed. No new driver process/native adapter was invoked and no execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete repository/prerequisite/environment/ancestor/output/direct-process adapters and final coordinator composition remain deliberately deferred for independent review; live native evidence and attended pixels also remain deferred, so the ticket stays open.
- 2026-08-30T09:38:01Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Rejected the exact build-driver candidate `5fb76d4c6cc733adb44647fb14fa65eb4e77c192` with eight P1 findings: syntax-only prerequisite claims, nonexact ref-pack acceptance, noncontinuous provenance/ancestor ownership, Game-only prior deletion, missing ADS proof for the runtime closure, cancellation cleanup loss, untrusted paired OS/temp environment facts and mutable dictionaries, and attestation consumption outside exception-independent disposal.
- 2026-08-30T09:38:01Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started correction of all eight rejected build-driver findings. The correction will add an immutable reviewed content manifest, continuously retained provenance and ancestor leases, exact three-output delete/absence/recreation proof, handle-bound default-stream checks, trusted OS/temp environment binding and freezing, cancellation cleanup aggregation, and exception-independent freezing/consumption/disposal of hostile attestation collections. Program remains inert and tests remain fake-only.
- 2026-08-30T09:52:49Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Corrected all eight rejected build-driver findings without composing or invoking execute. A code-owned immutable manifest now pins exact canonical paths and reviewed content hashes for `global.json`, the Game/Replay/Sim project-lock-assets inputs, SDK 8.0.423, the exact `Microsoft.NETCore.App.Ref/8.0.29/ref/net8.0` tree, and four exact Godot 4.7.1 package archives. One disposable provenance lease retains the clean candidate plus every repository/input/output ancestor chain continuously across three exact prior-output identity checks, deletion and absence proofs, direct bounded rebuild, three distinct recreation proofs, and retained runtime opening; revalidation refuses candidate/input change-and-restore attempts. Game, Replay, and Sim runtime handles now each require only `::$DATA`, stable single-link non-reparse snapshots, managed assembly name/hash/MVID, and distinct prior/new identities. The sanitized environment is copied into an immutable case-insensitive allowlist and binds `SystemRoot`/`WINDIR` plus `TEMP`/`TMP` to retained canonical directory identities before the process seam. The orchestrator freezes all attestation collections before validation/native use, disposes malformed or throwing attestations exactly once, and aggregates cancellation with both attestation and MSBuild cleanup failures. The zero-warning single-node eight-project build, 118 focused build/orchestrator cases, 663 applicable simulation cases excluding only the documented detached-worktree GoldenHistory tool-path group, all 134 checker cases, and repository checks passed. Program remains inert; no new production process/native adapter was called and no execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete provenance/environment/output/direct-process composition and live Windows evidence remain deliberately deferred for independent review, so the ticket stays open.
- 2026-08-30T10:04:01Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-review of exact correction `8f4ea95cc7ee091de3cbf3c31d5e24f07246c829` retained two P1 findings: package provenance pinned archives but not the extracted `.props`/`.targets`/DLL trees actually consumed, and prior Game/Replay/Sim outputs were inspected as values then deleted by path rather than through continuously retained identity-bound file/ancestor leases.
- 2026-08-30T10:04:01Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the exact two-finding correction. The package manifest will distinguish archive hashes from exact extracted-tree hashes and require continuously retained reparse-free directory/entry identities. Prior outputs will be acquired as three disposable file-and-ancestor leases whose same-handle identity-bound disposition proves each exact path absent, with reverse cleanup and failure aggregation. Program stays inert and all verification remains fake-only and headless.
- 2026-08-30T10:08:16Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed both residual P1 findings without composing execute. The immutable package manifest now distinguishes each exact `.nupkg` archive SHA-256 from the extracted 4.7.1 package-tree SHA-256, pins exact lower-case package roots and entry counts, documents the ordinal relative-path plus NUL plus raw-byte tree algorithm and exclusions, and requires the provenance lease to retain nonempty exact directory/entry identities across every rebuild revalidation. Regressions keep archives and normalized assets unchanged while changing an extracted SDK target or source-generator DLL, and also catch a package-tree change-and-restore during the build. The output boundary no longer exposes path deletion: it first acquires three disposable prior-file/ancestor leases, validates all identities before mutation, and each lease performs identity-bound retained-handle disposition plus exact-path absence proof while retaining its ancestor. Those leases remain owned through process completion and runtime opening, then close in reverse Game/Replay/Sim acquisition order before provenance; same-parent replacement, wrong-identity proof, partial deletion, and multiple cleanup failures all refuse and clean up deterministically. The zero-warning single-node eight-project build, 129 focused build/orchestrator cases, 674 applicable simulation cases excluding only the documented detached-worktree GoldenHistory tool-path group, all 134 checker cases, and repository checks passed. Program remains inert; no native/process adapter, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete provenance/output/process composition and live Windows evidence remain deferred for independent review, so the ticket stays open.
- 2026-08-30T10:12:33Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Re-review of `5b44b3983b424c5c5d19637a14ac5fa54e0dbf33` found one narrow cross-slot P1: recreated Game/Replay/Sim identities were each compared only with the corresponding prior slot, so a cross-slot permutation could reuse a retained prior identity.
- 2026-08-30T10:12:33Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started the narrow identity-set correction. Driver output-state validation and every retained runtime-handle open will require the complete recreated identity set to be disjoint from the complete prior identity set while preserving distinctness within both sets. Program remains inert and verification stays fake-only and headless.
- 2026-08-30T10:14:21Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed the cross-slot identity gap at both independent layers. Driver output-state validation now compares each recreated Game/Replay/Sim identity against the complete retained prior-identity set before runtime opening, while the runtime factory first requires three distinct nonempty prior identities and passes that full set into every retained-handle `OpenOne` attestation. Both layers still require distinct recreated identities. Two-slot swaps and three-cycles now refuse in both the output-state seam and retained-file factory. The zero-warning single-node eight-project build, 133 focused build/orchestrator cases, 678 applicable simulation cases excluding only the documented detached-worktree GoldenHistory tool-path group, all 134 checker cases, and repository checks passed. Program remains inert; no native/process adapter, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Existing concrete-composition and live-evidence deferrals remain unchanged, so the ticket stays open.
- 2026-08-30T10:15:00Z stage review start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Cold-reviewed the exact cumulative build-driver range `807e8ee611ef6c4a7e5f5f02e9ead305f9628a6d..03b387084655ecae2c329348ea9da0a9fd1fa4e6`, rechecking every prior finding and prioritizing complete prior/recreated identity-set disjointness at both the output-state and retained-handle layers.
- 2026-08-30T10:16:07Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Approved with no findings. Exact `/noAutoResponse` invocation, immutable admitted SDK/ref-pack/project/lock/assets/package archive and extracted-tree identities, retained provenance and trusted environment facts, three identity-bound prior-output deletion leases, full prior/recreated set disjointness, ADS-free stable PE/hash/MVID closure, direct-process resource contract, cancellation and cleanup aggregation, frozen attestation lifetime, and the inert Program all remain sound. Review was static while the user was gaming; no native API, build-driver process, Godot, renderer, GUI, browser, input, or visible process ran. Concrete provenance/environment/output/direct-process adapters, final coordinator/execute wiring, live Windows evidence, and attended monitor-4 pixels remain deferred, so the ticket stays open.
- 2026-08-30T10:18:37Z stage implementation start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started dormant concrete build-I/O adapters behind the inert launcher. The environment adapter will bind trusted retained SystemRoot/TEMP facts into a fresh exact thirteen-key non-inherited allowlist. The output adapter will retain exact reparse-free ancestor and prior-file handles, perform same-handle identity-bound disposition and handle-bound absence proof, validate recreated outputs nonmutatingly, and keep failed close/proof ownership retryable. All tests remain injected and fake-only; no native implementation, process, execute mode, Godot, GUI, renderer, browser, or input will run.
- 2026-08-30T10:36:40Z stage implementation end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Added dormant concrete build-environment and runtime-output adapters without composing Program or execute. The environment factory consumes only the already-retained canonical SystemRoot and TEMP facts, freezes the exact thirteen-key non-inherited allowlist, and returns a disposable lease retaining reparse-free ancestor handles plus exact empty DOTNET_CLI_HOME and MSBuildUserExtensionsPath directories; the build driver revalidates and retains that lease through process completion and runtime attestation, then aggregates its cleanup in the existing fail-closed order. The output adapter admits only exact-cased Game/Replay/Sim DLL paths, retains every ancestor without delete sharing, opens prior outputs once with READ+DELETE and read-only sharing, requires stable regular single-link default-stream-only identities, applies pinned FILE_DISPOSITION_INFO_EX class 21 flags on that same handle, proves delete-pending on the same identity, closes once, and proves leaf absence through the retained parent while retaining all ancestors. Recreated reads are nonmutating, identity-bound, stable, and use the same shared volume/file-ID formatter as runtime attestation. Production wrappers capture Win32 errors immediately and the independent bounded runtime-directory parser rejects overlap, overflow, and unbounded pages. Thirty-four dedicated fake-only adapter/ABI/race/cleanup cases, 167 focused build/orchestrator cases, 712 applicable simulation cases excluding only the documented detached-worktree GoldenHistory tool-path group, all 134 checker cases, the zero-warning eight-project cached single-node build, repository checks, ticket format, and whitespace passed. Program remains inert; no new P/Invoke adapter was called and no process, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete provenance and direct build-process adapters, final coordinator composition, live Windows evidence, and attended monitor-4 pixels remain deferred, so the ticket stays open.
- 2026-08-30T10:49:12Z stage review end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/review_hardening_035 — Rejected exact build-I/O candidate `8319460d77b79d972497deb2cc71a431a6038a3b` with three P1 findings: sampled empty environment directories did not continuously exclude transient child writes, the runtime directory parser admitted malformed/non-leaf names that could manufacture a false absence proof, and output ancestor ownership was recorded only after snapshot validation so a read failure plus close failure could escape deterministic aggregation.
- 2026-08-30T10:49:13Z stage correction start session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Started correction of all three rejected build-I/O findings. The environment lease will request identity-bound retained directory oplocks before its first empty observation and keep sticky break state through attestation; runtime directory records will be strict leaf names before absence use; and each ancestor handle will enter cleanup ownership immediately after open. Program remains inert and all verification remains injected, fake-only, and headless.
- 2026-08-30T10:51:01Z stage correction end session codex:01a0492f-57bc-7f32-87fa-1fbe5d483893/implement_hardening_035 — Closed all three build-I/O findings without composing or invoking native execution. Each exact empty DOTNET_CLI_HOME and MSBuildUserExtensionsPath leaf now opens overlapped and acquires a pending identity-bound RH directory oplock before the first empty observation; child namespace mutation requests a sticky break and remains blocked because the lease never acknowledges it. Revalidation proves both exclusions active and unbroken, and the driver retains them through build completion and runtime attestation. Bounded cancellation waits five seconds, failed cleanup keeps both exclusion and directory-handle ownership retryable, and tests cover pre-build/during-build breaks, transient add/remove, transfer through the returned attestation, and cleanup failure including retry after transfer. The independent runtime directory parser now uses strict UTF-16 and rejects zero-length, embedded-NUL, separator, ADS/colon, absolute, trailing-dot/space, malformed-surrogate, nonnormalized, control, invalid-Win32-character, and reserved DOS leaf names before any absence proof; an end-to-end deletion fake proves an unsafe name cannot manufacture absence. Output ancestor handles enter the owned cleanup list immediately after successful open, so snapshot failure plus a one-shot ambiguous close failure preserves and aggregates both errors without retry. Forty-eight dedicated adapter/ABI/race/cleanup cases, 184 focused build/orchestrator cases, 729 applicable simulation cases excluding only the documented detached-worktree GoldenHistory tool-path group, all 134 checker cases, the zero-warning cached eight-project single-node build, repository checks, ticket format, and whitespace passed. Program remains inert; no production oplock/P/Invoke adapter, deletion, process, execute mode, Godot, renderer, GUI, browser, input, or visible process ran. Concrete provenance/direct-process composition and live Windows evidence remain deferred, so the ticket stays open.
